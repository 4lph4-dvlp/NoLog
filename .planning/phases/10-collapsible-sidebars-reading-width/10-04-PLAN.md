---
phase: 10-collapsible-sidebars-reading-width
plan: 04
type: execute
wave: 4
depends_on: [10-01, 10-02, 10-03]
files_modified:
  - .planning/phases/10-collapsible-sidebars-reading-width/10-EVIDENCE.md
  - .planning/phases/10-collapsible-sidebars-reading-width/10-VALIDATION.md
autonomous: true
requirements: [SIDE-04, SIDE-05, SIDE-06, SIDE-07, SIDE-08, SIDE-09, SIDE-10, A11Y-02, A11Y-03, A11Y-04]

estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The collapsed-state content-column width is MEASURED, not derived: RESEARCH.md's identical gstack /browse procedure is re-run against the shipped collapse CSS and the both-collapsed figure is compared against D-04's derived 1304px, with any discrepancy reported rather than reconciled away."
    - "Both <aside> elements still stick on scroll after the transition CSS shipped, confirmed by scrolling a real long post and a real home page far enough to engage sticky — the one check that catches PITFALLS 9, which no lint or typecheck can see."
    - "The three RESEARCH-session pitfalls that are invisible to tsc and ESLint each have an explicit recorded observation: the true-0px collapse (Pitfall 6), sticky survival with the pinned row and the new overflow (Pitfalls 3/7/9), and that no code path attempts to hook into globals.css's dead html.transition-colors rule (Pitfall 8)."
    - "SIDE-10's stop-ship guard is verified three independent ways and all three are recorded: no publicly-prefixed Resend variable anywhere in the source, Layout.tsx carrying no client directive with the next build output inspected for its Server/Client marker, and the subscribe form observed rendering — or explicitly recorded UNEXERCISED if the local environment has no Resend credentials."
    - "Every one of 10-VALIDATION.md's 15 requirement rows plus its ROADMAP SC#5 row carries a real observation or an explicit UNEXERCISED note with the reason — never a blank, never an inferred pass."
    - "10-EVIDENCE.md reports the numbers actually read from the browser, and any figure that could not be obtained is labelled UNEXERCISED with the reason, following this project's own precedent (IMG-05's unexercised live half, 09-02)."

  artifacts:
    - path: ".planning/phases/10-collapsible-sidebars-reading-width/10-EVIDENCE.md"
      provides: "The phase's measured and observed evidence record: width measurements, the pitfall battery, the a11y battery, and the stop-ship battery"
      contains: "## Measured widths"
    - path: ".planning/phases/10-collapsible-sidebars-reading-width/10-VALIDATION.md"
      provides: "Per-Task Verification Map status column filled from real observations, and the nyquist_compliant frontmatter flag set on the evidence"
      contains: "nyquist_compliant"

  key_links:
    - from: ".planning/phases/10-collapsible-sidebars-reading-width/10-EVIDENCE.md"
      to: ".planning/phases/10-collapsible-sidebars-reading-width/10-VALIDATION.md"
      via: "each VALIDATION status cell cites the EVIDENCE section that establishes it"
      pattern: "10-EVIDENCE"
---

<!-- planner-discipline-allow: NEXT_PUBLIC_RESEND -->
<!-- planner-discipline-allow: use client -->

<objective>
Capture the evidence this phase cannot get from `tsc`, ESLint or `next build`. Five of the phase's risks are
delayed-onset CSS or runtime-behaviour traps that no automated tool in this repo can see, and D-09's
collapsed-state measurement was explicitly left open by RESEARCH.md because no shipped code existed to
measure. This plan closes both.

Purpose: turn the phase's fifteen requirement rows plus ROADMAP SC#5 from "implemented" into "observed",
following this project's own established practice (`07-EVIDENCE.md`, `08-CACHE-EVIDENCE.md`,
`09-EVIDENCE.md`) since no test framework exists and none may be added.
Output: `.planning/phases/10-collapsible-sidebars-reading-width/10-EVIDENCE.md`, and
`10-VALIDATION.md`'s status column filled from it.
</objective>

<execution_context>
@/home/alpha-pi/.claude/gsd-core/workflows/execute-plan.md
@/home/alpha-pi/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-collapsible-sidebars-reading-width/10-RESEARCH.md
@.planning/phases/10-collapsible-sidebars-reading-width/10-VALIDATION.md
@.planning/phases/10-collapsible-sidebars-reading-width/10-01-SUMMARY.md
@.planning/phases/10-collapsible-sidebars-reading-width/10-02-SUMMARY.md
@.planning/phases/10-collapsible-sidebars-reading-width/10-03-SUMMARY.md
</context>

<artifacts_this_phase_produces>
Every symbol Phase 10 creates. New symbols are excluded from drift verification.

**New files**
- `apps/web/src/lib/sidebar.ts` (plan 10-01)
- `apps/web/src/components/layout/SidebarShell.tsx` (plan 10-01)
- `apps/web/src/components/layout/SidebarToggleLeft.tsx` (plan 10-01)
- `apps/web/src/components/layout/SidebarToggleRight.tsx` (plan 10-02)
- `.planning/phases/10-collapsible-sidebars-reading-width/10-EVIDENCE.md` (this plan)

**Exported constants / types / functions (`apps/web/src/lib/sidebar.ts`)**
`SIDEBAR_BREAKPOINT_PX`, `SIDEBAR_STORAGE_KEY_PREFIX`, `SIDEBAR_ATTR_PREFIX`, `SIDEBAR_TRANSITION_ATTR`,
`SIDEBAR_PANEL_IDS`, `SidebarSide`, `SidebarPref`, `sidebarStorageKey`, `sidebarAttrName`,
`parseSidebarPref`, `readSidebarPref`, `writeSidebarPref`, `setSidebarAttr`, `initSidebarState`

**Exported components**
`SidebarShell` (+ `SidebarShellProps`), `SidebarToggleLeft` (+ `SidebarToggleLeftProps`),
`SidebarToggleRight` (+ `SidebarToggleRightProps`)

**CSS custom properties (overridden, not newly declared)**
`--sidebar-width`, `--profile-width`

**`@property` registrations (first in this repo)**
`@property --sidebar-width`, `@property --profile-width`

**New CSS class hook**
`.sidebar-grid`

**`data-*` attributes on `<html>`**
`data-sidebar-left`, `data-sidebar-right` (`"expanded"` | `"collapsed"`), `data-sidebar-transition`
(`"active"`, transient, click-originated only)

**HTML attribute newly used on the two panels**
`inert`

**`localStorage` keys**
`nolog:sidebar:left`, `nolog:sidebar:right` — values `"true"` / `"false"`; key absent means auto.

**DOM ids**
`sidebar-left-panel`, `sidebar-right-panel`

**Reader-facing strings (final, locked by UI-SPEC's Copywriting Contract)**
"Show search and categories", "Hide search and categories", "Show profile sidebar",
"Hide profile sidebar"
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1 (T10-04-1): Measure the collapsed state and run the stop-ship battery</name>
  <precondition>`npm run dev --prefix apps/web` starts and `http://localhost:3000/` answers 200 (a read-only GET is sufficient to confirm).</precondition>
  <files>.planning/phases/10-collapsible-sidebars-reading-width/10-EVIDENCE.md</files>
  <read_first>
    - `.planning/phases/10-collapsible-sidebars-reading-width/10-RESEARCH.md` — the "Code Context — Measured, Not Just Derived" section: the exact measurement method (`document.querySelector('main').parentElement.children` plus each child's `getBoundingClientRect().width`, plus the grid container's computed `grid-template-columns`) and its recommendation to re-run the identical procedure once the collapse CSS ships
    - `.planning/phases/10-collapsible-sidebars-reading-width/10-VALIDATION.md` — the Manual-Only Verifications table; every row in it must end up represented in the evidence file
    - `.planning/phases/10-collapsible-sidebars-reading-width/10-01-SUMMARY.md`, `10-02-SUMMARY.md`, `10-03-SUMMARY.md` — the numbers already recorded during implementation, so this plan reports agreement or disagreement rather than re-deriving
    - `apps/web/src/templates/default/Layout.tsx` — the file whose Server Component status the build-output inspection must confirm
  </read_first>
  <action>
Create `10-EVIDENCE.md` and fill its first two sections from real observation. Report the numbers actually
read; if a figure cannot be obtained, write `UNEXERCISED` with the reason rather than an inference — this
project's own precedent (IMG-05's unexercised live half, recorded in 09-02) is that an honest gap beats a
reasoned pass.

**Section `## Measured widths` (closes D-09's remaining half, SIDE-04).** Re-run RESEARCH.md's identical
procedure with `next dev` running and the gstack `/browse` skill: navigate to `http://localhost:3000/`, read
`document.querySelector('main').parentElement.children` and each child's `getBoundingClientRect().width`,
plus the grid container's computed `grid-template-columns`. Record a table with one row per state at a
1400px viewport — both expanded, left only collapsed, right only collapsed, both collapsed — and one row per
state at 1280px. Then state explicitly whether the both-collapsed figure equals D-04's derived **1304px**.
RESEARCH.md confirmed the expanded baseline by live measurement at four viewports (488/616/744/830) and left
the collapsed figure derived only. If the measured collapsed value differs from 1304px, report the
difference and its likely cause (a surviving gap, a residual track width, a rounding effect) — do not
silently adopt whichever number is prettier. Also record the measured `<article>` width on a post page in
each state, so the 1100px cap and the +236px payoff are evidence rather than arithmetic.

**Section `## Stop-ship battery (SIDE-10 / ROADMAP SC#3)`.** Three independent checks, all recorded with
their literal output:
1. `grep -rn 'NEXT_PUBLIC_RESEND' apps/web/src` — paste the result (expected: no lines). Also run it across
   the whole repo excluding `.planning/` and `node_modules/`, and record that too.
2. `grep -c 'use client' apps/web/src/templates/default/Layout.tsx` — paste the count (expected `0`). Then
   run `npm run build --prefix apps/web` and inspect the build output for the route/component marker
   confirming `Layout.tsx` was not pulled into a client bundle; paste the relevant output lines.
   Additionally record `grep -c 'SubscribeSection' apps/web/src/components/layout/SidebarShell.tsx`
   (expected `0`) and the same grep against `Layout.tsx` (expected at least `1`).
3. The live half: with `RESEND_API_KEY` and `RESEND_AUDIENCE_ID` present in the local environment, confirm
   the subscribe form renders in the right panel and submit it via the gstack `/browse` skill, recording the
   response. If those credentials are NOT available locally, record this sub-check as `UNEXERCISED — no
   Resend credentials in this environment` and additionally record the correctly-absent case (form absent
   with the variables unset, which is the shipped unconfigured-fork behaviour) so the gate's other direction
   is at least observed. Do not fabricate a submit result and do not create Resend credentials for this.

**Section `## Threshold and persistence (SIDE-05, SIDE-06)`.** With `localStorage` cleared, record the
observed state at 1281 / 1280 / 1279 px for each side; then record that a boundary sweep wrote no key;
then click each toggle and record both `localStorage` values, that each side restores independently after a
reload and after a route navigation, and that a sweep down to 800px does not override either explicit
preference (D-10).
  </action>
  <verify>
    <automated>test -f .planning/phases/10-collapsible-sidebars-reading-width/10-EVIDENCE.md &amp;&amp; for s in "## Measured widths" "## Stop-ship battery" "## Threshold and persistence"; do grep -qF "$s" .planning/phases/10-collapsible-sidebars-reading-width/10-EVIDENCE.md || { echo "MISSING: $s"; exit 1; }; done; echo OK</automated>
    <automated>grep -rn 'NEXT_PUBLIC_RESEND' apps/web/src; test $? -ne 0 &amp;&amp; test "$(grep -c 'use client' apps/web/src/templates/default/Layout.tsx)" = "0" &amp;&amp; test "$(grep -c 'SubscribeSection' apps/web/src/components/layout/SidebarShell.tsx)" = "0"</automated>
    <automated>test "$(grep -cE '1304|UNEXERCISED' .planning/phases/10-collapsible-sidebars-reading-width/10-EVIDENCE.md)" -ge 1 &amp;&amp; test "$(grep -cE '[0-9]+px' .planning/phases/10-collapsible-sidebars-reading-width/10-EVIDENCE.md)" -ge 8</automated>
    <automated>npm run build --prefix apps/web</automated>
  </verify>
  <acceptance_criteria>
    - `10-EVIDENCE.md` exists and contains the three headings `## Measured widths`, `## Stop-ship battery (SIDE-10 / ROADMAP SC#3)` and `## Threshold and persistence (SIDE-05, SIDE-06)`.
    - The width table holds a measured `<main>` value for all four sidebar combinations at 1400px and for the 1280px threshold case, each with the computed `grid-template-columns` string that was read.
    - The both-collapsed row states explicitly whether the measured value equals `1304px`, and names the difference and a candidate cause if it does not.
    - The evidence file contains the literal output of `grep -rn 'NEXT_PUBLIC_RESEND' apps/web/src` and of the client-directive count against `Layout.tsx`, plus the inspected `next build` output lines.
    - The live subscribe sub-check is either recorded with an actual submit result or explicitly labelled `UNEXERCISED` with the reason; it is never described as passing on inference.
    - Every numeric claim in the file is a number that was read from the browser or a command's output in this session — no figure is carried over from RESEARCH.md's pre-Phase-10 measurement without saying so.
    - `npm run build --prefix apps/web` succeeds.
  </acceptance_criteria>
  <done>The collapsed-state content width is measured against D-04's derived 1304px, the threshold and persistence behaviour is recorded from real observation, and SIDE-10's stop-ship guard is verified three independent ways with literal output — all written into `10-EVIDENCE.md` and committed.</done>
</task>

<task type="auto">
  <name>Task 2 (T10-04-2): Delayed-onset pitfall battery, a11y battery, and fill the validation map</name>
  <precondition>`npm run dev --prefix apps/web` starts and `http://localhost:3000/` answers 200, and at least one real published post is reachable at `/post/{id}` (a read-only GET is sufficient to confirm).</precondition>
  <files>.planning/phases/10-collapsible-sidebars-reading-width/10-EVIDENCE.md, .planning/phases/10-collapsible-sidebars-reading-width/10-VALIDATION.md</files>
  <read_first>
    - `.planning/phases/10-collapsible-sidebars-reading-width/10-RESEARCH.md` — Pitfalls 3, 6, 7 and 8, and Assumption A1 (own-element `overflow` versus an ancestor's) which the scroll test is the designated check for
    - `.planning/phases/10-collapsible-sidebars-reading-width/10-VALIDATION.md` — the Per-Task Verification Map (fill its Status column) and the Validation Sign-Off checklist
    - `.planning/phases/10-collapsible-sidebars-reading-width/10-EVIDENCE.md` — as written by task 1; append to it, do not overwrite
    - `apps/web/src/app/globals.css` — confirm the `html.transition-colors` rule is still present and untouched, and that no code was written to toggle it
  </read_first>
  <action>
Append the remaining evidence sections, then fill the validation map.

**Section `## Delayed-onset pitfall battery`.** Four observations no automated tool in this repo can make:
1. **Sticky survival (PITFALLS 9 / 3 / 7, RESEARCH Assumption A1, ROADMAP SC#5).** Scroll a real long post
   page and the home page far enough to engage sticky. Record whether BOTH `<aside>` elements still stick,
   and whether the pinned toggle row stays visible and does not overlap either panel's first interactive
   element at its sticky rest position. Repeat with each side collapsed. If either aside fails to stick,
   re-test with the transition rule neutralised to isolate cause, and record which it was — that is
   PITFALLS 9's own prescribed isolation step.
2. **True 0px collapse (Pitfall 6).** Record the measured collapsed track width for both sides. It must be
   exactly `0px`, not a nonzero value near the content's natural width, and no panel content may visibly
   escape the collapsed boundary during the transition.
3. **Dead-CSS confirmation (Pitfall 8).** Record `grep -rn 'transition-colors' apps/web/src --include='*.tsx' --include='*.ts'`
   and confirm no code adds the `transition-colors` class to `<html>`; confirm the `globals.css` rule is
   present and untouched. This closes the risk that an executor assumed live wiring where there is none.
4. **Reduced motion (A11Y-04).** With reduced motion emulated, record that a click-driven collapse is
   instant and that `data-sidebar-transition` never appears on `<html>`; then with the JS layer's guard
   bypassed by manually setting that attribute, record that the width change is still instant — proving the
   CSS layer holds on its own.

**Section `## Accessibility battery (A11Y-01…05)`.** Record, per side: `aria-expanded` and `aria-controls`
read off each toggle before and after a click; the accessibility-tree read of each panel expanded and
collapsed; a `press Tab` walk confirming a collapsed panel is skipped; `document.activeElement` after each
of the four focus-rescue cases (left/right x click/resize); and that each toggle's `aria-label` equals its
`title` in both states and that neither string contains `CONFIG.profile.name`.

**Section `## Mobile and visual battery (SIDE-08, SIDE-09, SIDE-07)`.** At 375x812 record that the mobile
stack is unchanged — Profile, Subscribe, Search, Categories in that order, the ThemeToggle visible, no
toggle row, and no horizontal scroll — and that a saved desktop collapsed preference has no visible mobile
effect. Screenshot the avatar toggle's ring in light and dark theme, expanded and collapsed, and record
that all four are visually identical. For SIDE-07, perform at least five cold reloads with a saved
collapsed preference at a viewport where the auto-state would differ, and record whether any wrong-state
flash was seen and whether the console produced a hydration warning; if the flash question cannot be
settled by eye, say so rather than declaring it clean. Finally, record the two UI backstop passes held out
in plans 10-01/10-02/10-03: a category name longer than 200px inside the left panel's fixed wrapper, a
deliberately long `CONFIG.profile.bio` inside the right panel's fixed wrapper, and a post containing a wide
table and a wide code block plus the longest real published title at the 1100px column — each in both
themes. Revert any temporary config or content change before committing and say so.

**Fill `10-VALIDATION.md`.** Replace the placeholder row in the Per-Task Verification Map with one row per
task ID (`T10-01-1`, `T10-01-2`, `T10-02-1`, `T10-02-2`, `T10-03-1`, `T10-03-2`, `T10-04-1`, `T10-04-2`),
each naming its plan, wave, requirement IDs, threat ref, secure behavior, test type, automated command, and
a Status of green / red / flaky based on what was observed — citing the `10-EVIDENCE.md` section that
establishes it. Add a row for ROADMAP SC#5's sticky check. Tick the Validation Sign-Off boxes that the
evidence actually supports and leave any unsupported box unticked with a one-line reason beneath the list.
Set `nyquist_compliant: true` in the frontmatter ONLY if every requirement row is green; otherwise leave it
`false` and list what is outstanding. Do not set `status: validated` — that lifecycle transition belongs to
`/gsd-validate-phase`.
  </action>
  <verify>
    <automated>for s in "## Delayed-onset pitfall battery" "## Accessibility battery" "## Mobile and visual battery"; do grep -qF "$s" .planning/phases/10-collapsible-sidebars-reading-width/10-EVIDENCE.md || { echo "MISSING: $s"; exit 1; }; done; echo OK</automated>
    <automated>test "$(grep -c '_pending_' .planning/phases/10-collapsible-sidebars-reading-width/10-VALIDATION.md)" = "0" &amp;&amp; for t in T10-01-1 T10-01-2 T10-02-1 T10-02-2 T10-03-1 T10-03-2 T10-04-1 T10-04-2; do grep -qF "$t" .planning/phases/10-collapsible-sidebars-reading-width/10-VALIDATION.md || { echo "MISSING: $t"; exit 1; }; done; echo OK</automated>
    <automated>grep -rn 'transition-colors' apps/web/src --include='*.tsx' --include='*.ts'; test $? -ne 0 &amp;&amp; test "$(grep -c 'html.transition-colors' apps/web/src/app/globals.css)" -ge 1</automated>
    <automated>npm run lint --prefix apps/web &amp;&amp; npm run build --prefix apps/web</automated>
    <human-check>
      The scroll test, the flash check, the ring comparison and the wide-content pass are irreducibly
      visual. Confirm each was actually looked at — not inferred — and that anything not looked at is
      labelled UNEXERCISED in `10-EVIDENCE.md` with the reason.
    </human-check>
  </verify>
  <acceptance_criteria>
    - `10-EVIDENCE.md` contains the headings `## Delayed-onset pitfall battery`, `## Accessibility battery (A11Y-01…05)` and `## Mobile and visual battery (SIDE-08, SIDE-09, SIDE-07)`.
    - The sticky-survival entry states for both asides, in both the expanded and each collapsed state, whether sticky still holds — and if any failed, which isolation step identified the cause.
    - The collapsed track width is recorded as an exact measured value for both sides.
    - `grep -rn 'transition-colors' apps/web/src --include='*.tsx' --include='*.ts'` returns no lines, and the `globals.css` rule is confirmed present — recorded verbatim in the evidence file.
    - The accessibility battery records `document.activeElement` for all four focus-rescue cases (left/right x click/resize), an accessibility-tree read for each panel in both states, and a Tab walk result.
    - `10-VALIDATION.md`'s Per-Task Verification Map contains no `_pending_` row and holds one row per task ID `T10-01-1` through `T10-04-2` plus a ROADMAP SC#5 row, each with a Status cell and a citation to a `10-EVIDENCE.md` section.
    - `nyquist_compliant` is `true` only if every requirement row is green; otherwise it is `false` and the outstanding items are listed. `status:` is left as `draft`.
    - Every UNEXERCISED entry carries a reason; no row is blank and no row claims a pass on inference.
    - `npm run lint --prefix apps/web` and `npm run build --prefix apps/web` both succeed.
  </acceptance_criteria>
  <done>The three delayed-onset pitfalls, the full accessibility battery, the mobile and visual checks and the four held-out backstop passes are each recorded as an observation or an explicit UNEXERCISED note, and `10-VALIDATION.md`'s map is filled with per-task statuses citing the evidence. Committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local dev server → the gstack `/browse` headless session | This plan drives a real browser against real local code; nothing untrusted crosses inward |
| operator's local environment → `10-EVIDENCE.md` | Secrets could be transcribed into a committed artifact if a battery output is pasted carelessly |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-02 | Information Disclosure | `10-EVIDENCE.md` (committed artifact) recording the stop-ship battery | high | mitigate | The battery records the PRESENCE or ABSENCE of variable NAMES and the literal grep output, never a variable's VALUE. `RESEND_API_KEY`'s value, the audience id, and any Resend API response containing a key or contact address must never be pasted into the evidence file; record status codes and shapes instead. Verified by reading the file before commit. |
| T-10-06 | Information Disclosure | temporary config edits made to exercise the D-14 fallback and the long-text backstops | medium | mitigate | Every temporary edit to `site.config.ts` or to Notion content is reverted before the commit and the revert is stated in the evidence file, matching the fault-injection-and-revert discipline this project used in Phases 8 and 9. `git status` is clean of `apps/web/src` changes at commit time. |
| T-10-01 | Tampering | `apps/web/src/lib/sidebar.ts` allowlist parse | medium | mitigate | Not modified by this plan; the battery additionally records the observed behaviour of a deliberately tampered `nolog:sidebar:left` value (a garbage string must yield the auto branch, not an attribute write), so the mitigation is observed rather than only asserted. |
| T-10-SC | Tampering | npm / package-manager installs | low | accept | This plan installs nothing and adds no dependency. `10-RESEARCH.md`'s Package Legitimacy Audit records zero new packages for the phase. |

## Non-applicable ASVS categories

| Category | Applies | Why not |
|---|---|---|
| V2 Authentication | no | No auth surface |
| V3 Session Management | no | No cookie or session state |
| V4 Access Control | no | No access-control logic |
| V6 Cryptography | no | No cryptographic operation |
</threat_model>

<verification>
- `npm run lint --prefix apps/web` and `npm run build --prefix apps/web` at the end of the plan; neither
  should change, since this plan writes only planning artifacts.
- The evidence file itself is the verification deliverable. Its standard is honesty, not greenness: an
  `UNEXERCISED` row with a reason is a pass for this plan; a row that claims an observation nobody made is
  a failure.
- `git status` must show no residual modification under `apps/web/src` at commit time — every fault
  injection and temporary config edit is reverted.
</verification>

<success_criteria>
- The collapsed-state content-column width is measured and compared against D-04's derived 1304px, closing
  the half of D-09's obligation RESEARCH.md could not close (SIDE-04).
- Both asides still stick on scroll with the pinned row and the new `overflow-hidden` in place
  (ROADMAP SC#5, PITFALLS 9, RESEARCH Assumption A1).
- The collapsed tracks reach exactly 0px (Pitfall 6) and no code attempts to hook into the dead
  `html.transition-colors` rule (Pitfall 8).
- SIDE-10's stop-ship guard is recorded three independent ways, with the live half either exercised or
  explicitly labelled UNEXERCISED.
- Every A11Y-01…05 behaviour is observed in a real browser, including all four focus-rescue cases.
- `10-VALIDATION.md`'s Per-Task Verification Map is filled with per-task statuses citing the evidence, and
  `nyquist_compliant` reflects what was actually observed.
- No secret value appears in any committed artifact and `apps/web/src` is clean of temporary edits.
</success_criteria>

<output>
Create `.planning/phases/10-collapsible-sidebars-reading-width/10-04-SUMMARY.md` when done.
Record, verbatim: the measured both-collapsed width and whether it matched 1304px, the sticky-test result
for both asides in all three collapse states, the collapsed track widths, whether SIDE-10's live half was
exercised, the count of green versus UNEXERCISED requirement rows, and the final `nyquist_compliant` value
with its justification.
</output>
