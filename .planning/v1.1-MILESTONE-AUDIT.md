---
milestone: v1.1
audited: 2026-08-14T00:00:00Z
status: tech_debt
scores:
  requirements: 25/25
  phases: 4/4
  integration: 25/25
  flows: 3/3
gaps:
  requirements: []
  integration: []
  flows: []
nyquist:
  compliant_phases: 0
  partial_phases: 0
  not_validated_phases: 4
  missing_phases: 0
  overall: not-validated
tech_debt:
  - phase: 07-content-failure-isolation-live-diagnosis
    items:
      - "VERIFICATION.md is stale: body still reads `**Status:** human_needed` while its frontmatter reads `status: passed`, and SC#3/SC#4 are recorded PRESENT_BEHAVIOR_UNVERIFIED. Both were closed later by Phase 8 plan 03 under D-15 (recorded in STATE.md); 07-UAT.md reads `status: complete`. No CORRECTION section was appended."
      - "VALIDATION.md is `status: draft` — never reconciled by validate-phase (NOT-VALIDATED, a coverage TODO not a compliance failure)."
  - phase: 08-content-rendering-fix
    items:
      - "VERIFICATION.md is stale: truth #6 (CONT-05's fetch-failed sentence) recorded PRESENT_BEHAVIOR_UNVERIFIED. Closed later by Phase 8's own UAT (recorded in STATE.md as '08-VERIFICATION gap 3 closed'); 08-UAT.md reads `status: complete`. No CORRECTION section was appended."
      - "The four `08-*-SUMMARY.md` files carry no `requirements_completed` frontmatter, so CONT-03 and CONT-05 do not appear in the SUMMARY cross-check source."
      - "VALIDATION.md is `status: draft` — NOT-VALIDATED."
  - phase: 09-thumbnail-freshness
    items:
      - "Two source-only assurances accepted by the operator rather than observed: IMG-03's host-allowlist guard (Notion picks the presign host, so an off-allowlist case cannot be constructed from real data) and IMG-05's live half (no external-thumbnail post exists in the operator's database)."
      - "VALIDATION.md is `status: draft` — NOT-VALIDATED."
  - phase: 10-collapsible-sidebars-reading-width
    items:
      - "Two UAT items accepted-but-unobserved (`result: accepted`, not `pass`): E7's wide-table/code-block backstop and SC#5's home-page sticky depth. Both blocked by real content scarcity on the operator's live site; production Notion content was deliberately not mutated."
      - "SIDE-10's live submit round trip and A11Y-04's simultaneous dual-bypass were unexercised during automated verification for tool/policy reasons; both were subsequently closed by operator UAT on 2026-08-14."
      - "VALIDATION.md is `status: draft` with `nyquist_compliant: false` deliberately held — NOT-VALIDATED by the tool's classification, but the `false` is an intentional honest record, not an omission."
      - "IN-01 from code review (inline ref-callback closures reallocated every render) left unfixed as functionally harmless."
---

# Milestone v1.1 — Audit Report

**Milestone:** v1.1 Live Blog Bug Fixes & Reading Width
**Phases:** 7, 8, 9, 10 (4/4 complete)
**Audited:** 2026-08-14
**Status:** `tech_debt` — all requirements satisfied, zero functional blockers, accumulated documentation debt needs review

---

## Verdict

**All 25 requirements are satisfied and all cross-phase integration is verified.** There are no
functional gaps, no orphaned requirements, no broken flows, and no unsatisfied requirements — so the
FAIL gate does not fire.

The status is `tech_debt` rather than `passed` because the milestone carries real, non-minimal
documentation debt: two phase VERIFICATION.md files are stale (they still record gaps that later work
closed), four phases have unreconciled VALIDATION.md files, and six verification items across Phases 9
and 10 are accepted-by-operator rather than observed. None of these is a code defect. All of them are
things a future reader would otherwise mistake for open work.

---

## Requirements Coverage (3-source cross-reference)

25/25 satisfied. Sources: REQUIREMENTS.md traceability table, each phase's VERIFICATION.md, and each
SUMMARY.md's `requirements_completed` frontmatter.

| REQ-ID | Phase | Traceability | VERIFICATION | SUMMARY frontmatter | Final |
|---|---|---|---|---|---|
| CONT-01 | 7 | Complete | passed | `07-01` | **satisfied** |
| CONT-02 | 7 | Complete | passed | *absent* | **satisfied** ¹ |
| CONT-03 | 8 | Complete | passed | *absent* | **satisfied** ¹ |
| CONT-04 | 7 | Complete | passed | `07-01`, `07-02` | **satisfied** |
| CONT-05 | 8 | Complete | passed | *absent* | **satisfied** ¹ |
| IMG-01…IMG-05 | 9 | Complete | passed | `09-01`, `09-03`, `09-04` | **satisfied** |
| SIDE-01…SIDE-10 | 10 | Complete | passed | `10-01`…`10-04` | **satisfied** |
| A11Y-01…A11Y-05 | 10 | Complete | passed | `10-01`…`10-04` | **satisfied** |

**¹ Resolved manually, per the status-determination matrix's `passed + missing frontmatter → partial
(verify manually)` rule.** CONT-02, CONT-03 and CONT-05 appear in no SUMMARY's
`requirements_completed` field because `07-03-SUMMARY.md` and all four `08-*-SUMMARY.md` files omit
that frontmatter key entirely. Manual verification confirms all three are genuinely satisfied:

- **CONT-02** — `07-EVIDENCE.md` records the captured production evidence (403 + `text/html` Cloudflare
  page against a named Production deployment), the six-candidate discriminating table, and a named
  verdict. REQUIREMENTS.md's own closure note cites it.
- **CONT-03** — the User-Agent fix shipped and was observed working across all three public posts over
  four passes spanning ~9 minutes on the deployed site (`08-CACHE-EVIDENCE.md`).
- **CONT-05** — the no-content branch was calibrated against a real empty Notion page in plan 08-02;
  the fetch-failed branch was observed directly in Phase 8's UAT with the content leg forced to throw.

**Orphan detection:** none. Every REQ-ID in the traceability table appears in at least one phase's
VERIFICATION.md.

---

## Phase Verification

| Phase | Status | Score | Note |
|---|---|---|---|
| 07 Content Failure Isolation & Live Diagnosis | passed | 3/4 must-haves | Two truths recorded PRESENT_BEHAVIOR_UNVERIFIED; **both closed later** — see Stale Artifacts |
| 08 Content Rendering Fix | passed | 8/11 must-haves | One truth recorded PRESENT_BEHAVIOR_UNVERIFIED; **closed later** — see Stale Artifacts |
| 09 Thumbnail Freshness | passed | 6/6 must-haves | Two source-only assurances accepted by operator |
| 10 Collapsible Sidebars & Reading Width | passed | 15/15 must-haves | Two items accepted-but-unobserved; UAT closed 2026-08-14 |

---

## Cross-Phase Integration

**PASSED.** 25+ connections verified wired, 3 E2E flows traced complete, 0 orphaned exports, 0 orphaned
API routes, 0 missing dependencies, 0 blockers.

The planning docs asserted the three defect fixes were file-disjoint. That held, and the two places
where phases genuinely do share a file were checked directly:

- **Phase 9 ↔ Phase 10 both edit `templates/default/PostPage.tsx`.** Phase 9 owns the hero thumbnail
  (line 93, `<PostThumbnail variant="hero">`); Phase 10 owns the article cap (line 26,
  `max-w-[1100px]`). Both present, no conflict — the hero renders through the Phase 9 proxy path inside
  the capped article.
- **Phase 10 restructured `templates/default/Layout.tsx` into `SidebarShell` slots.** Every pre-existing
  component still renders: `SearchBar`, `CategoryList`, `Profile` (now a `<div>`, the landmark fix), and
  v1.0 Phase 3's `SubscribeSection` in `rightSlot`. Nothing was orphaned.
- **Phase 9's `/api/thumbnail/[id]`** is still consumed from all four `default` surfaces after the layout
  rework.
- **Phase 8's content path** (`app/post/[id]/page.tsx` → `lib/notion-x.ts` → `NotionPageRenderer`) is
  untouched by Phase 10.
- **Phase 7's diagnostic scaffolding teardown (D-19) is complete**: `/api/diagnose-page` absent,
  `describeFetchFailure()` removed, while `isDiagnosticsEnabled()` and `lib/post-availability.ts` are
  preserved and still wired, and the leg-named logging that satisfies CONT-01 is intact.

**E2E flows traced:** home feed → thumbnails → proxy route → display; post detail → content fetch →
three-way branch → capped column; sidebar collapse → focus rescue → inert → subscribe form still
rendering and interactive.

---

## Nyquist Coverage

| Phase | VALIDATION.md | status | nyquist_compliant | Classification | Action |
|---|---|---|---|---|---|
| 07 | exists | draft | false | NOT-VALIDATED | `/gsd-validate-phase 7` |
| 08 | exists | draft | false | NOT-VALIDATED | `/gsd-validate-phase 8` |
| 09 | exists | draft | false | NOT-VALIDATED | `/gsd-validate-phase 9` |
| 10 | exists | draft | false | NOT-VALIDATED | `/gsd-validate-phase 10` |

All four are **NOT-VALIDATED**, not PARTIAL — `status: draft` means validate-phase never reconciled the
file, so `nyquist_compliant: false` is not authoritative for phases 7-9. This is a coverage TODO, not a
compliance failure.

**Phase 10 is a deliberate exception worth stating plainly:** its `nyquist_compliant: false` was set
*intentionally* by plan 10-04, with a written rationale, because four rows carry explicitly recorded
UNEXERCISED sub-items. Running `/gsd-validate-phase 10` would promote `status` to `validated` but should
**not** flip `nyquist_compliant` to `true` until those items are genuinely observed.

This project has zero test infrastructure by explicit constraint (REQUIREMENTS.md D-07 lineage), so
Nyquist's automated-coverage model has limited purchase here — verification across all four phases was
source assertions, `npm run lint`, `npm run build`, and direct browser observation.

---

## Stale Artifacts (the main finding)

Two VERIFICATION.md files record gaps that **later work closed**, but were never back-updated. Both
closures are recorded in `STATE.md` and corroborated by the phases' own UAT files reading
`status: complete`.

| File | What it still says | What actually happened |
|---|---|---|
| `07-VERIFICATION.md` | Body header reads `**Status:** human_needed` (frontmatter says `passed`). SC#3 (chrome failure doesn't blank the body) and SC#4 (public post never 404s on a content failure) recorded PRESENT_BEHAVIOR_UNVERIFIED — "structural half verified, live half unexercised". | Phase 8 plan 03 closed both by direct observation under D-15: an env-gated forced throw in the chrome leg left the body rendering at HTTP 200 with exactly one `[PostPage:chrome]` line and zero `[PostPage:recordMap]` lines; and both 404-vs-unavailable directions were exercised. `07-UAT.md` reads `status: complete`. |
| `08-VERIFICATION.md` | Truth #6 — CONT-05's fetch-failed sentence — recorded PRESENT_BEHAVIOR_UNVERIFIED ("no plan or SUMMARY records ever inducing a real content-leg failure"). Also complains REQUIREMENTS.md is stale at `[~] implemented, deploy-unverified`. | Phase 8's UAT observed the sentence directly with the content leg forced to throw (HTTP 200, correct sentence, zero occurrences of the no-content sentence, renderer not entered). REQUIREMENTS.md was subsequently updated and now reads `[x] … closed 2026-08-10 on the deployed site` — so that complaint is itself stale. `08-UAT.md` reads `status: complete`. |

**Neither is a functional gap.** The remedy is a `## CORRECTION` section appended to each file — the
same pattern this project already used in `01-VERIFICATION.md` and `09-VERIFICATION.md`.

---

## Accepted Unobserved Items

Six items across two phases were accepted rather than observed. Each has a stated, non-fabricated
reason. None is a code defect.

| Item | Phase | Why unobservable | Disposition |
|---|---|---|---|
| IMG-03 host-allowlist guard firing | 9 | Notion chooses the presign host; an off-allowlist case cannot be constructed from real data | Operator accepted source assurance |
| IMG-05 live half | 9 | No external-thumbnail post exists in the operator's database | Operator accepted source assurance |
| SIDE-10 live submit round trip | 10 | Harness classifier blocked a side-effecting PII write to production Resend | **Closed by operator UAT 2026-08-14** |
| A11Y-04 simultaneous dual-bypass | 10 | CDP `Emulation.setEmulatedMedia` not on `/browse`'s allowlist | **Closed by operator UAT 2026-08-14** |
| E7 wide table / code block at the 1100px cap | 10 | The operator's 3 posts contain zero tables and zero code blocks; production Notion was deliberately not mutated | Accepted as residual risk |
| SC#5 home-page sticky depth | 10 | Home page `scrollHeight` is 900px at a 900px viewport — no room for the post page's 2000px test | Accepted; the post page passed at full rigor |

The last two resolve themselves as the blog gains content.

---

## Build & Lint

`npm run build --prefix apps/web` → exit 0. `npm run lint --prefix apps/web` → exit 0.
Zero new npm dependencies across all four phases. `packages/core` unchanged by v1.1.

---

## Recommended Follow-ups (none blocking)

1. Append a `## CORRECTION` section to `07-VERIFICATION.md` and `08-VERIFICATION.md` recording the later
   closures, so a future reader does not re-derive the same confusion this audit had to.
2. Run `/gsd-validate-phase 7`…`10` to reconcile the four draft VALIDATION.md files — keeping Phase 10's
   `nyquist_compliant: false` until its two accepted items are genuinely observed.
3. Re-check E7 and SC#5 once the blog has a post containing a wide table or code block, and enough posts
   for the home page to scroll.
4. Refresh the codebase map — `/gsd-map-codebase --paths .gitignore,README.md,README_KR.md,TODOS.md,package-lock.json,package.json,packages/core` (a drift advisory has been firing since Phase 10 planning; unrelated to this milestone's work).

---

*Audited by Claude (`/gsd-audit-milestone 1.1`) — 3-source requirements cross-reference, cross-phase
integration check via `gsd-integration-checker`, Nyquist scan, and a pre-close open-artifact audit.*
