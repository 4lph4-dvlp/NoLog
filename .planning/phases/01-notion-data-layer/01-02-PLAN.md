---
phase: 01-notion-data-layer
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/client.ts
autonomous: true
gap_closure: true
requirements: [DATA-01]

user_setup:
  - service: notion
    why: "The CODE fix + build + grep gate are fully automatable without live Notion. Only the carried-forward BEHAVIORAL proof (mark-then-requery) needs a live Notion test workspace whose Status property uses the canonical capital 'Status' name. This is the SAME credential/dashboard gate as 01-01-PLAN.md's user_setup — reused/referenced here, not re-invented."
    env_vars:
      - name: NOTION_TOKEN
        source: "See 01-01-PLAN.md user_setup — notion.so/my-integrations → your integration → Internal Integration Secret"
      - name: NOTION_DATABASE_ID
        source: "See 01-01-PLAN.md user_setup — the test database URL's 32-char id segment; the database's Status property must be named with the canonical capital 'Status'"

must_haves:
  truths:
    # --- the one NEW truth this gap-closure plan's work asserts (CR-01 closure) ---
    - "Both getPosts() and getUnemailedPublicPosts() Notion query filters use the property key \"Status\" (capital S) — the canonical casing mapPageToPost()'s getSelect(page, \"Status\", ...) at client.ts:114 uses as its primary key and types.ts (line 33-34) documents — so a workspace whose Status property follows the documented canonical name receives its intended filtered result set instead of a Notion 400 (closes CR-01; DATA-01, ROADMAP SC#2)."
    - "The @4lph4/nolog-core package still builds clean and apps/web still typechecks after the change — a string-literal edit introduces no type regression."
    - "mapPageToPost()'s getSelect(page, \"Status\", \"status\") extractor (which reads a per-page property and can legitimately fall back to a legacy lowercase key client-side) is left UNCHANGED — only the two server-side query filters (which Notion matches case-sensitively, no fallback possible) are corrected."
    # --- carried-forward, now UNBLOCKED by this fix; live-Notion → human at verify time ---
    - { statement: "Against a live Notion test workspace with a canonical Status property, `npx tsx packages/core/scripts/verify-phase-1.ts` reaches and passes the mark-then-requery assertion instead of erroring on the query itself — the CR-01 fix is the precondition that unblocks the DATA-01/DATA-02 live verification carried forward from 01-01.", verification: backstop }
  artifacts:
    - "packages/core/src/client.ts — getPosts() filter (formerly at client.ts:226) and getUnemailedPublicPosts() compound-filter status clause (formerly at client.ts:259) both use property: \"Status\""
  key_links:
    - "getUnemailedPublicPosts() / getPosts() query filter `property` value → must exactly match the Notion database's canonical Status property name (server-side filter is case-sensitive; unlike the extractor family, a single Notion filter request cannot fall back to a second key) — same primary casing mapPageToPost()'s getSelect uses"
---

<objective>
Close the single code-provable gap 01-VERIFICATION.md found in Phase 1 (status: gaps_found, 5/10): **CR-01** — both `getPosts()` and `getUnemailedPublicPosts()` hardcode the Notion query-filter property key as lowercase `"status"`, while every other read of that property in the same file treats capitalized `"Status"` as canonical (`mapPageToPost()`'s `getSelect(page, "Status", "status")` at client.ts:114, and `types.ts`'s JSDoc). Notion's `/v1/databases/{id}/query` filter matches the `property` name case-sensitively with **no** server-side fallback, so for any workspace using the documented canonical `Status` name both methods receive a 400 instead of the intended result set. Change both filters' `property` value from `"status"` to `"Status"`.

This is a single, minimal, in-scope bug fix — one string literal per filter site, nothing else. It does NOT re-plan or re-execute 01-01 (left untouched as the historical record), add a schema-detection layer, add a dual-casing fallback query, add automated tests, or touch WR-02 (all explicitly out of this gap-closure pass's scope).

**Single-task / no separate tracer decision (TRACER_MODE=true):** the architecture this phase touches was already proven end-to-end by 01-01's tracer (query → mark → requery, wired through client.ts). This gap-closure changes only two string literals inside that already-proven, already-wired path — the whole fix IS the thinnest slice, so a single `type="auto"` task is correct here rather than a separate tracer task.

Purpose: A forker whose Notion database follows the codebase's own documented canonical `Status` property name gets a working blog (getPosts() powers the home/category/search read path) AND a working unemailed-posts query (getUnemailedPublicPosts(), DATA-01) instead of a 400 on both.
Output: `packages/core/src/client.ts` with both query filters using `property: "Status"`; package builds clean; the DATA-01/DATA-02 live mark-then-requery verification carried forward from 01-01 is now unblocked to run.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-notion-data-layer/01-VERIFICATION.md
@.planning/phases/01-notion-data-layer/01-REVIEW.md
@packages/core/src/client.ts
@packages/core/src/types.ts
</context>

## Artifacts this phase produces

Every symbol this plan MODIFIES (nothing is created), so a source-grounding/drift pass does not flag them:

| Symbol | Kind | File | Change |
|--------|------|------|--------|
| `NologClient.getPosts()` | public method — its query filter | `packages/core/src/client.ts` (~line 226) | Filter `property` value `"status"` → `"Status"`. Pre-existing sibling defect; site-wide home/category/search read path. |
| `NologClient.getUnemailedPublicPosts()` | public method — its compound-filter status clause | `packages/core/src/client.ts` (~line 259) | First `and`-clause `property` value `"status"` → `"Status"` (DATA-01). The `{ property: "Emailed", checkbox: { equals: false } }` clause is untouched. |

**No other symbol changes.** `mapPageToPost()`'s `getSelect(page, "Status", "status")` extractor (client.ts:114), the sort clauses, the `Emailed` filter clause, `types.ts`, `apps/web/src/types/index.ts`, the barrel export, and both verify scripts are all left exactly as 01-01 left them.

## Scope decision — getPosts() is fixed here, NOT deferred

01-VERIFICATION.md CR-01 and 01-REVIEW.md CR-01 both name `getPosts()` (line 226) and `getUnemailedPublicPosts()` (line 259) as the **same root-cause defect in the same file**. `getPosts()` is technically pre-existing and outside DATA-01/02/04's original REQ-ID scope, but it is fixed in this same task rather than deferred, because: (a) it is a one-character change to an adjacent line in the same file; (b) leaving it lowercase would keep the site's **primary** home/category/search data-fetch path broken for any canonical-schema forker, sitting right next to the "fixed" sibling; (c) fixing only DATA-01's method would leave an internally-inconsistent file. This is a deliberate in-scope inclusion, explicitly documented here (not a silent expansion).

## CRITICAL — RESEARCH.md Pattern 1 guidance is SUPERSEDED

`01-RESEARCH.md` "Pattern 1" and `01-PATTERNS.md` line 47 (`property: "status", // NOTE: lowercase "status" — codebase-specific, mirror exactly`) instruct mirroring the lowercase `"status"` key. **That guidance is exactly what produced CR-01 and is superseded by 01-VERIFICATION.md's CR-01 finding — do NOT follow it.** RESEARCH.md inferred the lowercase key must be correct *from* the pre-existing `getPosts()` filter, without recognizing that filter itself is the bug. Every other property-access convention in the same file (the `getSelect`/`getRichText`/etc. extractor family, always canonical-capital-primary) and `types.ts`'s own JSDoc point to `"Status"` as canonical. The executor reads RESEARCH.md/PATTERNS.md via 01-01's context — this note exists so that inherited guidance does not re-introduce the defect.

## Flagged Assumptions & edge-coverage cross-check (surfaced, not silently dropped)

- **Spec-less edge-probe (6 unresolved items):** cross-checked against 01-01-PLAN.md's existing `must_haves` — all already resolved there and unchanged by this fix, so NOT re-authored (avoiding duplication per the plan contract):
  - DATA-01 *ordering* → 01-01 backstop truth (created_time ascending / oldest-first).
  - DATA-01 *empty* → 01-01 truth (returns `[]`, never null/throw, when nothing matches).
  - DATA-01 *adjacency* → not applicable: the filter is an exact boolean/select equality (`Emailed=false`, `Status=public`), no "touching/merging" boundary exists.
  - DATA-04 *idempotency* → 01-01 truth (repeat `markEmailed` sends identical PATCH, no-op success).
  - DATA-04 *concurrency* → 01-01 backstop truth (no guard beyond Notion last-write-wins, per REQUIREMENTS.md Out-of-Scope).
  - DATA-02 *unclassified* → 01-01 already surfaced this as an open assumption; its SUMMARY confirmed no distinct edge beyond the idempotent-write contract. Nothing new added by a filter-key fix.
  The single NEW edge this plan's own work introduces — the corrected `"Status"` filter-key behavior — IS authored into `must_haves.truths` above.
- **Prohibitions (two-stage prose recall):** minted **zero** bespoke prohibitions for this narrow scope. The one candidate — "an error message could leak the Notion token / Authorization header" — is referred to canon (01-01-PLAN.md `<threat_model>` T-1-01, unchanged), not minted here; this plan adds no new error-message surface at all.

<tasks>

<task type="auto">
  <name>Task 1: Correct the Notion query-filter property key from lowercase "status" to canonical "Status" in both getPosts() and getUnemailedPublicPosts()</name>
  <reversibility rating="reversible">A filter `property` string literal — reverting is a one-character change with no data-shape, migration, or external-contract implication. No checkpoint needed.</reversibility>
  <precondition>Running the carried-forward manual verify (verify-phase-1.ts) requires a live Notion test workspace (NOTION_TOKEN + NOTION_DATABASE_ID) whose Status property is named with the canonical capital "Status" — required ONLY to prove the fix behaviorally, NOT to compile the code or pass the automated build+grep gate. Same live-Notion gate as 01-01-PLAN.md's user_setup.</precondition>
  <files>packages/core/src/client.ts</files>
  <read_first>
    - packages/core/src/client.ts — the ONLY file modified. Read: `getPosts()` and its single-clause filter near line 226; `getUnemailedPublicPosts()` and its compound `and` filter near line 259; and `mapPageToPost()`'s `getSelect(page, "Status", "status")` at line 114 — which establishes `"Status"` (capital) as the canonical PRIMARY key and lowercase `"status"` as only a client-side fallback for the per-page extractor.
    - packages/core/src/types.ts — lines 33-34: the JSDoc on `Post.status` names it "the `Status` (select) property", confirming capital-S is the documented canonical property name.
    - .planning/phases/01-notion-data-layer/01-VERIFICATION.md — the CR-01 gap: the exact defect, why it is statically provable (independent of live Notion), and why only the two server-side filters (not the extractor) need fixing.
    - .planning/phases/01-notion-data-layer/01-REVIEW.md — CR-01, the original critical finding this closes.
    - NOTE (do NOT act on): 01-RESEARCH.md Pattern 1 and 01-PATTERNS.md line 47 tell you to mirror lowercase `"status"`. That guidance is SUPERSEDED by CR-01 — ignore it. See the "RESEARCH.md Pattern 1 guidance is SUPERSEDED" section above.
  </read_first>
  <action>
    Change the `property` field value in BOTH Notion query filters from the lowercase literal `"status"` to the canonical `"Status"` (capital S). There are exactly two sites, both confirmed by grep:

    - Site 1 — `getPosts()` near line 226: the single-clause filter of shape `{ property: <value>, select: { equals: "public" } }`. Set `<value>` to `"Status"`.
    - Site 2 — `getUnemailedPublicPosts()` near line 259: the FIRST clause of the compound `and` filter, of shape `{ property: <value>, select: { equals: "public" } }`. Set `<value>` to `"Status"`.

    Do NOT touch any of the following: the `{ property: "Emailed", checkbox: { equals: false } }` clause in `getUnemailedPublicPosts()`; `mapPageToPost()`'s `getSelect(page, "Status", "status")` at line 114 (its lowercase SECOND argument is a legitimate client-side extractor fallback key and must stay — the extractor CAN fall back per-page; a server-side filter CANNOT); the `sorts` clauses; and every other method (getPost, getCategories, getBlocks, patchPage, markEmailed, the extractor family, the two error classes).

    Why this is the correct and complete fix (rationale for the executor, not a scope to expand): Notion's `/v1/databases/{id}/query` filter matches the `property` name case-sensitively and has no server-side fallback, so the filter key must equal the database's actual canonical property name — which this codebase documents (types.ts) and reads everywhere else (getSelect primary arg) as `"Status"`. Do NOT add a schema-detection layer, a dual-casing/try-both-keys fallback query, an env-var toggle, or any "v1 / for now / static / placeholder" scaffolding — that is over-engineering the project explicitly did not ask for and conflicts with its "Minimal" core value. The minimal correct fix is the two string literals only.

    getPosts() is fixed here alongside getUnemailedPublicPosts() (not deferred) — see the "Scope decision" section above for why both are in scope.

    Do NOT add any code comment that contains the literal `property: "Status"` (a comment with that literal would inflate the count-2 verification gate; a brief comment naming the canonical convention without that exact substring is fine).

    Then rebuild so `dist/` reflects the corrected source before any manual run: `npm run build --workspace=@4lph4/nolog-core` (Pitfall 4 — verify-phase-1.ts runs against `dist/`, not `src/`; a stale `dist/` would hide the change). If `node_modules/` is absent in the environment, run `npm install` against the existing lockfile first (as 01-01 did) — a dependency-restore, not a new package.
  </action>
  <verify>
    <automated>npm run build --workspace=@4lph4/nolog-core && test "$(grep -v '^\s*//' packages/core/src/client.ts | grep -c 'property: "Status"')" = "2"</automated>
    <manual>Carried forward (live-Notion, human — same gate as 01-01): `npx tsx packages/core/scripts/verify-phase-1.ts` against a live Notion test DB whose Status property uses the canonical capital name → stdout reaches "PASS: post correctly excluded after markEmailed()" WITHOUT a 400 on the query itself (proving the CR-01 fix unblocks DATA-01/DATA-02, ROADMAP SC#1-3).</manual>
  </verify>
  <acceptance_criteria>
    - Both the `getPosts()` filter and the `getUnemailedPublicPosts()` compound-filter's status clause use the literal `property: "Status"` (capital S).
    - On non-comment lines of `packages/core/src/client.ts`, `grep -c 'property: "Status"'` returns exactly `2` (getPosts + getUnemailedPublicPosts); no status-`select` filter still uses the lowercase key.
    - `mapPageToPost()`'s `getSelect(page, "Status", "status")` at line 114 is unchanged — the client-side extractor fallback is deliberately preserved.
    - The `{ property: "Emailed", checkbox: { equals: false } }` clause, both `sorts` clauses, and all other methods (getPost, getCategories, getBlocks, patchPage, markEmailed, extractors, error classes) are byte-unchanged.
    - `npm run build --workspace=@4lph4/nolog-core` compiles clean and refreshes `dist/`; `npx tsc --noEmit -p apps/web/tsconfig.json` exits 0.
    - No newly-added code comment contains the literal `property: "Status"` (keeps the count-2 gate meaningful).
    - Manual (carried forward, human, live-Notion-gated): verify-phase-1.ts reaches the mark-then-requery PASS without erroring on the query.
  </acceptance_criteria>
  <done>Both Notion query filters use the canonical `"Status"` property key, so `getPosts()` (site-wide read path) and `getUnemailedPublicPosts()` (DATA-01) both query successfully against a workspace following the documented canonical schema instead of receiving a 400 — CR-01 closed. Build + typecheck clean; the DATA-01/DATA-02 live mark-then-requery verification carried forward from 01-01 is now unblocked to run against real credentials.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Unchanged from 01-01-PLAN.md. This plan edits two string literals inside an already-wired query path — it introduces NO new trust boundary, NO new package, and NO new error-message surface.

| Boundary | Description |
|----------|-------------|
| `NologClient` → Notion REST API | Unchanged — same outbound authenticated query; only the filter's `property` value string differs. |

## STRIDE Threat Register

No new threats. The three threats from 01-01-PLAN.md are carried forward unchanged and remain correctly dispositioned; nothing in this plan's scope alters them.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1-01 (carried forward) | Information Disclosure | `NotionCapabilityError` / `MissingEmailedPropertyError` messages | high | mitigate | Unchanged — this plan touches no error constructor or message; the existing "no token/Authorization header in any error message" invariant (verified in 01-01) still holds. |
| T-1-02 (carried forward) | Tampering | `pageId` interpolated in `patchPage()` | low | accept | Unchanged — `patchPage()` is not modified; `pageId` still originates only from `getUnemailedPublicPosts()` results. |
| T-1-SC (carried forward) | Tampering | npm/pip/cargo installs | low | accept | Zero new packages this plan (only a possible dependency-restore `npm install` against the existing lockfile). Nothing to audit. |

**ASVS level:** 1. **Block-on threshold:** high — T-1-01 (`high`) is dispositioned `mitigate` and unchanged, so the gate clears with no new work.
</threat_model>

<verification>
Phase-level checks for this gap-closure (automated gate is real; behavioral proof stays manual-by-design — no test framework in this repo, VALIDATION.md is the authority):

1. `npm run build --workspace=@4lph4/nolog-core` compiles clean AND non-comment `grep -c 'property: "Status"'` on client.ts returns exactly 2 (automated gate — proves both filter keys corrected with no type regression).
2. `npx tsc --noEmit -p apps/web/tsconfig.json` exits 0 (no downstream type breakage).
3. Carried forward (human, live Notion — same credential gate as 01-01): `npx tsx packages/core/scripts/verify-phase-1.ts` reaches the mark-then-requery PASS without a 400 on the query, against a workspace whose Status property uses the canonical capital name (ROADMAP SC#1-3, DATA-01/DATA-02).

Explicitly NOT in scope for this pass (per gap_closure directive): WR-02 (missing-property detection regex — classified behavior_unverified, needs live Notion, left as-is); adding automated tests (REQUIREMENTS.md Out-of-Scope excludes a test framework); re-checking DATA-01/02/04 in REQUIREMENTS.md (happens at verify-time after a human confirms the live round-trip, not at plan time).
</verification>

<success_criteria>
- Both Notion query filters (`getPosts()` and `getUnemailedPublicPosts()`) use `property: "Status"` — CR-01 closed; grep gate returns exactly 2 non-comment occurrences.
- The extractor fallback (`getSelect(page, "Status", "status")`, line 114), the `Emailed` clause, the sorts, and all other methods are untouched.
- Package builds clean; `apps/web` typechecks clean; no token/header ever added to any message (T-1-01 unchanged).
- The DATA-01/DATA-02 live mark-then-requery verification carried forward from 01-01 is unblocked (reaches the query without a 400 for a canonical-schema workspace).
- 01-01-PLAN.md is left byte-untouched (historical record); no automated test framework added; WR-02 left as-is.
</success_criteria>

<output>
Create `.planning/phases/01-notion-data-layer/01-02-SUMMARY.md` when done. In it, record: confirmation that both filter sites now use `property: "Status"` and the grep-count-2 gate passed; confirmation the extractor fallback + Emailed clause + sorts + all other methods were left unchanged; the build/typecheck results; and — if a live Notion workspace was reachable — whether verify-phase-1.ts reached the mark-then-requery PASS without a query 400 (otherwise note it remains carried-forward pending human credentials, same as 01-01).
</output>
