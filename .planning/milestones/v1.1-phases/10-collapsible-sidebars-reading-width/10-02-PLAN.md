---
phase: 10-collapsible-sidebars-reading-width
plan: 02
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - apps/web/src/components/layout/SidebarToggleRight.tsx
  - apps/web/src/components/layout/SidebarShell.tsx
  - apps/web/src/app/globals.css
  - apps/web/src/components/Profile.tsx
autonomous: true
requirements: [SIDE-02, SIDE-03, SIDE-04, SIDE-09, A11Y-01, A11Y-05]

estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 2
  confidence: low

must_haves:
  truths:
    # ── Goal-backward ──
    - "A reader clicks the circular profile-image button and the right grid track goes 240px -> 0px while <main> grows by exactly 240px; clicking again restores it."
    - "The two sides are genuinely independent: collapsing the right leaves the left untouched, and with both collapsed <main> is 1304px at a >=1400px viewport (the two 32px gaps survive, D-04)."
    - "The avatar toggle carries an always-on accent ring in both the expanded and the collapsed state, so it reads as a control rather than decoration (SIDE-09 / D-12)."
    - "If CONFIG.profile.avatarUrl is missing or fails to load, the button still renders and still operates — a lucide-react User icon appears inside the same 36px circle with the ring unchanged (D-14)."
    - "The right panel is a single complementary landmark: Profile.tsx's root is a <div>, so the only landmark for that side is the <aside> that carries id=\"sidebar-right-panel\", the aria-controls target and the future inert target."
    - "The avatar toggle's accessible name is action-phrased (\"Show profile sidebar\" / \"Hide profile sidebar\"), is passed identically to aria-label and title, and shares no wording with the Profile card avatar's own alt text."
    # ── EDGE probe lift: SIDE-09 (idempotency, concurrency) ──
    - "SIDE-09 idempotency: toggling the right side any number of times leaves the ring's width, colour and offset byte-identical — only aria-expanded and the label string differ between states, never the ring treatment."
    - "SIDE-09 concurrency: the ring is declared in the button's static class string, not derived from transition or collapse state, so a click landing mid-transition cannot produce an intermediate or missing ring."
    # ── EDGE probe lift: A11Y-01 (concurrency) ──
    - "A11Y-01 concurrency: aria-expanded flips to its target value in the same synchronous handler that flips the <html> attribute, so during the 200ms transition the button already reports the state the panel is moving to — assistive tech is never told a value the panel will not reach."
    # ── EDGE probe lift: A11Y-05 (empty, encoding, idempotency, concurrency) ──
    - "A11Y-05 empty: the avatar toggle's <img> uses alt=\"\" (decorative), so the button's accessible name is computed from aria-label alone and there is no second text alternative to collide with it."
    - "A11Y-05 encoding: distinctness from CONFIG.profile.name is established by wording, not by string comparison — the two locked labels share no substring with the configured name, so the guarantee holds under any byte/code-point/grapheme or normalization definition of equality and survives a forker renaming their profile."
    - "A11Y-05 idempotency: one label binding is passed to both aria-label and title in each state, so the two attributes can never drift apart on re-render."
    - "A11Y-05 concurrency: the label is derived from the same collapsed state as aria-expanded, so the tooltip wording and the reported state always agree."
    # ── UI Considerations lift: E2 avatar toggle (5 rows, all explicit) ──
    - "UI E2 empty: absent media resolves to D-14's fallback — a centred lucide-react User icon at w-5 h-5 on a bg-surface-active backdrop inside the same 36px circle, with the accent ring unchanged."
    - "UI E2 loading: before hydration a fixed 36px circular placeholder renders. After hydration the image is CONFIG.profile.avatarUrl (/avatar.png), a local build-bundled static asset served through next/image — no spinner or skeleton, matching what Profile.tsx's own 80px avatar already does in production."
    - "UI E2 error: a failed load fires the image's native onError, which flips local state to the same D-14 icon fallback as the empty case. One mechanism covers both; no retry and no error message."
    - "UI E2 populated: the normal state is the 36px circle with an object-cover image, alt=\"\", and the always-on ring-2 ring-accent ring-offset-2 ring-offset-background cue present identically whether the right panel is expanded or collapsed."
    - "UI E2 long-text: the avatar toggle renders no text node; alt is deliberately empty and the accessible name is a fixed literal, so text length cannot affect layout."
    # ── UI Considerations lift: E4 pinned sticky toggle row (4 rows, all explicit) ──
    - "UI E4 loading: the row is server-rendered markup and each of its three children carries its own fixed-size mounted-guard placeholder, so the row's height and its children's positions are stable from first paint through hydration — no reflow, no cumulative layout shift."
    - "UI E4 error: the row is layout chrome with no async work of its own; no error state exists."
    - "UI E4 overflow: the row renders only at md (>=768px) and holds three ~36px controls plus one 8px gap (about 116px) inside a container at least 736px wide, so it cannot overflow. justify-between deliberately decouples the cells from the animating grid tracks so the hamburger's hit area never shrinks toward 0 as the left track collapses."
    - "UI E4 long-text: the row contains no text node — three icon-only controls."
    # ── UI Considerations lift: E6 right collapsible panel (7 explicit + 1 backstop) ──
    - "UI E6 empty: with the two Resend variables unset, SubscribeSection returns null server-side and the panel holds only the Profile card — the shipped, deliberate unconfigured-fork state, which the panel must render correctly."
    - "UI E6 loading: the panel is server-rendered with the page; no data-loading state. The collapse transition is its only in-flight state and the panel is inert throughout it."
    - "UI E6 error: a failed subscribe submission is handled by SubscribeForm's existing error rendering, unchanged by this phase; the panel's own structure has no failure mode."
    - "UI E6 populated: the Profile card above the subscribe block inside a fixed w-[240px] inner wrapper in a sticky self-start <aside>; Profile.tsx's root becomes a <div> with an identical className — zero visual change."
    - "UI E6 partial: the configured-vs-unconfigured split is the panel's only partial-data axis and is covered by the empty row; no other field of this panel is optional at render time."
    - "UI E6 overflow: the same Pitfall 6 mechanism as the left panel — min-w-0 overflow-hidden on the <aside> plus a fixed w-[240px] inner wrapper clips the avatar, bio text and subscribe form rather than reflowing them during collapse."
    - "UI E6 zero-one-many: the social-links row's 0/1/many handling stays Profile.tsx's existing activeLinks.length > 0 guard, unchanged by this phase."
    - statement: "UI E6 long-text: a long CONFIG.profile.bio or CONFIG.profile.greeting inside the fixed w-[240px] inner wrapper must not push the wrapper wider or escape overflow-hidden. Held for a real visual pass with a deliberately long bio string, in both themes, expanded and mid-collapse."
      verification: backstop

  prohibitions:
    - statement: "MUST NOT present the avatar toggle as an account or profile menu — no dropdown, no menu semantics (no aria-haspopup, no role=\"menu\"), no account-like affordance such as a caret or status dot. NoLog has no user accounts and implying one misleads the reader about what the site does."
      category: transparency

  artifacts:
    - path: "apps/web/src/components/layout/SidebarToggleRight.tsx"
      provides: "Circular profile-image disclosure button for the right panel, with accent ring cue and icon fallback on image failure"
      exports: ["SidebarToggleRight"]
    - path: "apps/web/src/components/layout/SidebarShell.tsx"
      provides: "Both sides' tri-state now live: the per-side machinery is invoked for right as well as left"
      contains: "SidebarToggleRight"
    - path: "apps/web/src/app/globals.css"
      provides: "@property registration for --profile-width and the collapsed-right custom-property override"
      contains: "@property --profile-width"
    - path: "apps/web/src/components/Profile.tsx"
      provides: "Root element demoted from <aside> to <div> so the outer panel is the sole landmark"
      contains: "flex flex-col items-center text-center gap-4 p-6 bg-surface"

  key_links:
    - from: "apps/web/src/components/layout/SidebarToggleRight.tsx"
      to: "apps/web/src/site.config.ts"
      via: "imports CONFIG directly for profile.avatarUrl — safe because site.config.ts contains no process.env read"
      pattern: "CONFIG.profile.avatarUrl"
    - from: "apps/web/src/components/layout/SidebarShell.tsx"
      to: "apps/web/src/app/globals.css"
      via: "the right toggle's click writes data-sidebar-right, which the new attribute-scoped override reads to zero --profile-width"
      pattern: "data-sidebar-right"
    - from: "apps/web/src/components/layout/SidebarToggleRight.tsx"
      to: "apps/web/src/components/layout/SidebarShell.tsx"
      via: "aria-controls points at SIDEBAR_PANEL_IDS.right, the id on the <aside> SidebarShell renders"
      pattern: "SIDEBAR_PANEL_IDS.right"
---

<!-- planner-discipline-allow: use client -->
<!-- planner-discipline-allow: SubscribeSection -->
<!-- planner-discipline-allow: <aside -->

<objective>
Expand the proven slice to the right side: build the circular profile-image toggle, invoke plan 10-01's
per-side machinery a second time for `"right"`, add the right side's CSS override, and demote
`Profile.tsx`'s root `<aside>` to a `<div>` so the collapsible panel is an unambiguous single landmark.

After this plan both sides are independently collapsible and the phase's collapse mechanism is complete;
only the accessibility sequencing (plan 10-03) and the prose cap (plan 10-03) remain.

Purpose: SIDE-02, SIDE-03 and SIDE-09 close here, and A11Y-05's distinctness guarantee becomes real
(D-12 ring, D-13 wording, D-14 fallback).
Output: `apps/web/src/components/layout/SidebarToggleRight.tsx`, plus edits to `SidebarShell.tsx`,
`globals.css` and `Profile.tsx`.
</objective>

<execution_context>
@/home/alpha-pi/.claude/gsd-core/workflows/execute-plan.md
@/home/alpha-pi/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-collapsible-sidebars-reading-width/10-CONTEXT.md
@.planning/phases/10-collapsible-sidebars-reading-width/10-RESEARCH.md
@.planning/phases/10-collapsible-sidebars-reading-width/10-PATTERNS.md
@.planning/phases/10-collapsible-sidebars-reading-width/10-UI-SPEC.md
@.planning/phases/10-collapsible-sidebars-reading-width/10-01-SUMMARY.md
</context>

<interfaces>
<!-- Contracts the executor needs. Do not go exploring for these. -->

`apps/web/src/components/layout/SidebarToggleRight.tsx` (NEW — this plan creates it):

```typescript
interface SidebarToggleRightProps {
  collapsed: boolean;
  mounted: boolean;
  onToggle: () => void;
}
export function SidebarToggleRight(props: SidebarToggleRightProps): JSX.Element;
```

Already shipped by plan 10-01 and consumed here unchanged:

```typescript
// apps/web/src/lib/sidebar.ts
export const SIDEBAR_PANEL_IDS: Record<"left" | "right", string>;  // right -> "sidebar-right-panel"
export function setSidebarAttr(side: SidebarSide, collapsed: boolean): void;
export function readSidebarPref(side: SidebarSide): SidebarPref;
export function writeSidebarPref(side: SidebarSide, collapsed: boolean): void;
```

The two analogs to copy from (read them, do not re-derive):

```typescript
// apps/web/src/components/PostThumbnailImage.tsx — the onError fallback shape
export function PostThumbnailImage(props: { src: string; alt: string; variant: "card" | "hero" }): JSX.Element;
// apps/web/src/site.config.ts — plain literals, no process.env read anywhere, safe to import client-side
export const CONFIG: { profile: { name: string; avatarUrl: string; bio: string; greeting: string }; /* ... */ };
```
</interfaces>

<artifacts_this_phase_produces>
Every symbol Phase 10 creates. New symbols are excluded from drift verification.

**New files**
- `apps/web/src/lib/sidebar.ts` (plan 10-01)
- `apps/web/src/components/layout/SidebarShell.tsx` (plan 10-01)
- `apps/web/src/components/layout/SidebarToggleLeft.tsx` (plan 10-01)
- `apps/web/src/components/layout/SidebarToggleRight.tsx` (this plan)
- `.planning/phases/10-collapsible-sidebars-reading-width/10-EVIDENCE.md` (plan 10-04)

**Exported constants / types / functions (`apps/web/src/lib/sidebar.ts`)**
`SIDEBAR_BREAKPOINT_PX`, `SIDEBAR_STORAGE_KEY_PREFIX`, `SIDEBAR_ATTR_PREFIX`, `SIDEBAR_TRANSITION_ATTR`,
`SIDEBAR_PANEL_IDS`, `SidebarSide`, `SidebarPref`, `sidebarStorageKey`, `sidebarAttrName`,
`parseSidebarPref`, `readSidebarPref`, `writeSidebarPref`, `setSidebarAttr`, `initSidebarState`

**Exported components**
`SidebarShell` (+ `SidebarShellProps`), `SidebarToggleLeft` (+ `SidebarToggleLeftProps`),
`SidebarToggleRight` (+ `SidebarToggleRightProps`)

**CSS custom properties (overridden, not newly declared)**
`--sidebar-width`, `--profile-width`

**`@property` registrations (first in this repo)**
`@property --sidebar-width` (plan 10-01), `@property --profile-width` (this plan)

**New CSS class hook**
`.sidebar-grid`

**`data-*` attributes on `<html>`**
`data-sidebar-left`, `data-sidebar-right` (`"expanded"` | `"collapsed"`), `data-sidebar-transition`
(`"active"`, transient, click-originated only)

**`localStorage` keys**
`nolog:sidebar:left`, `nolog:sidebar:right` — values `"true"` / `"false"`; key absent means auto.

**DOM ids**
`sidebar-left-panel`, `sidebar-right-panel`

**Reader-facing strings (final, locked by UI-SPEC's Copywriting Contract)**
"Show search and categories", "Hide search and categories", "Show profile sidebar",
"Hide profile sidebar"
</artifacts_this_phase_produces>

<flagged_assumptions>
Probe-surfaced rows this plan carries as flagged assumptions rather than authored predicates. Both landed
`unclassified` because the requirement text is an affordance/independence statement with no data-shape cue
for a data-shape taxonomy to classify. Each names how the plan verifies it by other means.

| Req | Probe verdict | Why unclassified | How this plan still verifies it |
|---|---|---|---|
| SIDE-02 | `unresolved` / `unclassified` | "Reader can collapse and expand the right sidebar using a circular button showing the site owner's profile image" describes an affordance, not a data shape | Task 1 acceptance criteria assert the circular geometry, the image source and the fallback; task 2's `human-check` observes the collapse; UI-SPEC's five authored E2 rows cover its states |
| SIDE-03 | `unresolved` / `unclassified` | "Reader can collapse one sidebar while leaving the other expanded" is a cross-component independence property with no shape to bite on | Task 2's `human-check` exercises all four left/right combinations and records the measured `<main>` width for each, including the 1304px both-collapsed figure |
</flagged_assumptions>

<tasks>

<task type="auto">
  <name>Task 1 (T10-02-1): Avatar toggle button — ring cue, decorative image, icon fallback</name>
  <files>apps/web/src/components/layout/SidebarToggleRight.tsx</files>
  <read_first>
    - `apps/web/src/components/ThemeToggle.tsx` — the `mounted` guard shape and the `aria-label === title` convention
    - `apps/web/src/components/PostThumbnailImage.tsx` — the exact `useState(false)` + ternary-render `onError` fallback shape to reuse (this is the nearest precedent for D-14)
    - `apps/web/src/components/Profile.tsx` lines 63-75 — the `relative w-N h-N rounded-full overflow-hidden bg-surface-active` wrapper and the `next/image` `fill` + `object-cover` + `sizes` prop shape
    - `apps/web/src/components/layout/SidebarToggleLeft.tsx` — the sibling written by plan 10-01; match its props shape and its focus-visible convention
    - `apps/web/src/site.config.ts` — confirm `profile.avatarUrl` and `profile.name`, and confirm there is no `process.env` read in the file
    - `.planning/phases/10-collapsible-sidebars-reading-width/10-UI-SPEC.md` — Component Contract 2 (every ring/hover/focus value is locked there) and the Copywriting Contract
  </read_first>
  <action>
Create the circular profile-image disclosure button. First line is the client directive, byte-identical to
`ThemeToggle.tsx:1`. Props per `<interfaces>`.

Imports: `useState` from react, `Image` from `next/image`, `User` from `lucide-react`, `CONFIG` from
`@/site.config`, `SIDEBAR_PANEL_IDS` from `@/lib/sidebar`. Importing `CONFIG` directly is deliberate and
safe — `site.config.ts` holds plain literals with no environment read, so it does not need threading
through `Profile.tsx`.

Button element: `<button type="button">` with `aria-expanded={!collapsed}`,
`aria-controls={SIDEBAR_PANEL_IDS.right}`, and one `label` binding — `collapsed ? "Show profile sidebar" :
"Hide profile sidebar"` — passed to BOTH `aria-label` and `title`. These are the final locked strings; do
not paraphrase them. They must share no wording with `CONFIG.profile.name`, which is what A11Y-05's
distinctness turns on.

Chrome, verbatim from UI-SPEC Component Contract 2: `relative w-9 h-9 rounded-full overflow-hidden
cursor-pointer` for the 36px circle, plus the always-on cue `ring-2 ring-accent ring-offset-2
ring-offset-background`, plus `hover:ring-accent-hover`, plus
`focus-visible:outline-none focus-visible:ring-[3px] focus-visible:ring-accent focus-visible:ring-offset-2
focus-visible:ring-offset-background`. The ring is declared in the static class string and is identical in
both the expanded and collapsed states — its whole purpose is to say "this is a control" precisely when the
panel is gone and the reader is looking for the way back, so it must not dim or disappear on collapse. No
`scale`/`transform` on hover: a transform near a `position: sticky` ancestor is unnecessary risk
(Pitfall 3).

Image: `<Image src={CONFIG.profile.avatarUrl} alt="" fill sizes="36px" className="object-cover" />` — `fill`
inside the `relative` button, `alt` deliberately empty so the accessible name comes from `aria-label` alone
and cannot duplicate the Profile card avatar's own alt. Do not pass `priority` (this is chrome, not the
profile card's hero avatar).

Fallback (D-14): a local `const [failed, setFailed] = useState(false)`; the image carries
`onError={() => setFailed(true)}`. When `failed` is true, render instead
`<div className="absolute inset-0 flex items-center justify-center bg-surface-active">` containing
`<User className="w-5 h-5 text-text-secondary" strokeWidth={1.5} />`. No retry, no error text, no boundary
change — this component is already a Client Component, which is exactly why D-14 costs nothing here. The
ring, the 36px circle, and the button's click behaviour are unchanged in the fallback state, so the control
stays operable when a fork deletes or mis-points `/avatar.png`.

Mounted guard: when `mounted` is false, render the same 36px circular chrome with the ring, holding an
empty `<div className="w-9 h-9" />` and no `onClick` — a fixed-size placeholder so the row's geometry never
shifts at hydration.

Do NOT add `aria-haspopup`, `role="menu"`, a caret, a status dot, or any dropdown affordance: this is a
disclosure button for a sidebar, not an account menu, and NoLog has no accounts.
  </action>
  <verify>
    <automated>npm run lint --prefix apps/web</automated>
    <automated>test "$(grep -c 'aria-haspopup\|role="menu"' apps/web/src/components/layout/SidebarToggleRight.tsx)" = "0" &amp;&amp; test "$(grep -c 'alt=""' apps/web/src/components/layout/SidebarToggleRight.tsx)" -ge 1 &amp;&amp; test "$(grep -c 'ring-2 ring-accent' apps/web/src/components/layout/SidebarToggleRight.tsx)" -ge 1</automated>
    <automated>test "$(grep -c 'Show profile sidebar' apps/web/src/components/layout/SidebarToggleRight.tsx)" -ge 1 &amp;&amp; test "$(grep -c 'Hide profile sidebar' apps/web/src/components/layout/SidebarToggleRight.tsx)" -ge 1 &amp;&amp; test "$(grep -c 'onError' apps/web/src/components/layout/SidebarToggleRight.tsx)" -ge 1</automated>
    <human-check>
      Not exercisable until task 2 wires the button into the row. Deferred to task 2's battery; record
      here only that the component compiles and lints clean.
    </human-check>
  </verify>
  <acceptance_criteria>
    - The file's first line is the client directive, matching `ThemeToggle.tsx:1` byte for byte.
    - `grep -c 'aria-haspopup' apps/web/src/components/layout/SidebarToggleRight.tsx` and the same grep for `role="menu"` both return `0`.
    - The image carries `alt=""` and no `priority` prop; `sizes="36px"` and `fill` are both present.
    - Exactly one `label` binding is passed to both `aria-label` and `title`; the two literals `Show profile sidebar` and `Hide profile sidebar` both appear, and neither contains the value of `CONFIG.profile.name`.
    - `ring-2 ring-accent ring-offset-2 ring-offset-background` appears in the button's static class string — not inside a conditional expression keyed on `collapsed`.
    - `onError` sets local state that renders a `User` icon at `w-5 h-5` over `bg-surface-active`, inside the unchanged 36px circle with the ring still applied.
    - `aria-expanded={!collapsed}` and `aria-controls={SIDEBAR_PANEL_IDS.right}` are both present; the id is read from the shared module, not written as a literal.
    - `npm run lint --prefix apps/web` succeeds.
  </acceptance_criteria>
  <done>The circular profile-image toggle exists, carries its always-on accent ring and both locked labels, and falls back to an icon inside the same operable button when the avatar fails to load — committed.</done>
</task>

<task type="auto">
  <name>Task 2 (T10-02-2): Wire the right side — shell invocation, CSS override, single landmark</name>
  <files>apps/web/src/components/layout/SidebarShell.tsx, apps/web/src/app/globals.css, apps/web/src/components/Profile.tsx</files>
  <read_first>
    - `apps/web/src/components/layout/SidebarShell.tsx` — as written by plan 10-01; the per-side machinery, the `prefRef`, `handleToggle`, the pinned row's right cell, and the right `<aside>` that already carries `id={SIDEBAR_PANEL_IDS.right}`
    - `apps/web/src/app/globals.css` — the `@property --sidebar-width` block and `html[data-sidebar-left="collapsed"]` rule added by plan 10-01, plus the `:root` declarations at lines 41-42 and the `.dark` block boundary at lines 63-97 (the new rules go OUTSIDE `.dark`)
    - `apps/web/src/components/Profile.tsx` — the full file; line 63's root element and its exact `className`
    - `apps/web/src/components/layout/SidebarToggleRight.tsx` — as written by task 1
    - `.planning/phases/10-collapsible-sidebars-reading-width/10-RESEARCH.md` — the `aria-controls` Target section (Open Question 5's resolution) and Pitfall 6
  </read_first>
  <action>
Three edits that make the right side live.

**1. `apps/web/src/app/globals.css`.** Immediately after the `@property --sidebar-width` block, add
`@property --profile-width { syntax: '<length>'; inherits: true; initial-value: 240px; }`. Immediately after
the existing `html[data-sidebar-left="collapsed"]` rule, add
`html[data-sidebar-right="collapsed"] { --profile-width: 0px; }`. Both go outside the `.dark` block, at the
same nesting level as `:root` — these two custom properties are declared only under `:root` and are not
redefined in `.dark`, which is exactly why one attribute-scoped override works in both themes with no
per-theme duplication. Do not add a `@theme inline` bridge. Do not change the existing transition rule; it
already targets `.sidebar-grid` and therefore covers both tracks.

**2. `apps/web/src/components/layout/SidebarShell.tsx`.** Invoke plan 10-01's per-side machinery a second
time for `"right"` — the same state pair, the same `prefRef` mirror, the same mount-effect read of
`document.documentElement.getAttribute(sidebarAttrName("right"))` and `readSidebarPref("right")`, and the
same `handleToggle("right")` path. Do not fork or duplicate the logic: if plan 10-01 left it as a
parameterized helper, call it again; if it is inline, extract it into one per-side helper first and call it
twice, so the tri-state model exists exactly once. The `matchMedia` `change` handler already iterates the
sides — confirm it now covers `right` too and still skips any side whose `prefRef` is non-null.

Import `SidebarToggleRight` and render it inside the row's existing right cell, immediately to the RIGHT of
`<ThemeToggle />` inside the `flex items-center gap-2` div, so the order left-to-right is `ThemeToggle` then
avatar. This is D-05's placement: each sidebar's control sits on the same edge as the sidebar it operates.
Pass `collapsed`, `mounted` and the right side's toggle handler.

**3. `apps/web/src/components/Profile.tsx`.** Change the root element from `<aside>` to `<div>` — opening
and closing tag — keeping the `className` byte-identical
(`flex flex-col items-center text-center gap-4 p-6 bg-surface border border-border rounded-2xl shadow-sm transition-colors`).
Zero visual change: a `<div>` accepts the same Tailwind classes. This is a prerequisite, not a tidy-up:
today this element nests a `complementary` landmark inside `SidebarShell`'s right panel landmark, so
assistive tech reports two regions where there is one collapsible panel, and hiding only the outer one from
the accessibility tree needs the boundary to be unambiguous. Add a one-line comment naming that reason.
Change nothing else in the file — the avatar, the `activeLinks.length > 0` guard and every other element
stay exactly as they are.
  </action>
  <verify>
    <automated>npm run lint --prefix apps/web</automated>
    <automated>test "$(grep -c '<aside' apps/web/src/components/Profile.tsx)" = "0" &amp;&amp; test "$(grep -c 'flex flex-col items-center text-center gap-4 p-6 bg-surface border border-border rounded-2xl shadow-sm transition-colors' apps/web/src/components/Profile.tsx)" -ge 1</automated>
    <automated>test "$(grep -c '@property --profile-width' apps/web/src/app/globals.css)" -ge 1 &amp;&amp; test "$(grep -c 'data-sidebar-right="collapsed"' apps/web/src/app/globals.css)" -ge 1 &amp;&amp; test "$(grep -c 'SidebarToggleRight' apps/web/src/components/layout/SidebarShell.tsx)" -ge 1</automated>
    <automated>test "$(grep -c 'SubscribeSection' apps/web/src/components/layout/SidebarShell.tsx)" = "0" &amp;&amp; grep -rn 'NEXT_PUBLIC_RESEND' apps/web/src; test $? -ne 0</automated>
    <automated>npm run build --prefix apps/web</automated>
    <human-check>
      With `npm run dev --prefix apps/web` running at 1400x900, and `localStorage` cleared first, record the
      measured `<main>` width for all four combinations (the executor may drive this with the gstack
      `/browse` skill and must report the numbers it actually read, not the expected ones):
      1. both expanded — 864px; left only collapsed — 1064px; right only collapsed — 1104px;
         both collapsed — 1304px. Confirm the computed `grid-template-columns` matches each case.
      2. Collapsing the right leaves the left exactly where it was, and vice versa (SIDE-03).
      3. `localStorage` holds two independent keys after clicking both toggles, and each side restores
         independently after a reload.
      4. Screenshot the avatar toggle in light AND dark theme, expanded AND collapsed: the accent ring is
         present and visually identical in all four, and hover shifts only its colour.
      5. Temporarily repoint `CONFIG.profile.avatarUrl` at a path that 404s, reload, and confirm the button
         still renders with the `User` icon inside the ring and still collapses the panel on click. Revert
         the config change before committing and say so in the SUMMARY.
      6. Run an accessibility-tree read of the right panel and confirm exactly ONE complementary region for
         that side, not two.
      7. Confirm the subscribe form still renders in the right panel with the Resend vars set (or is
         correctly absent without them) and record which case was observed.
    </human-check>
  </verify>
  <acceptance_criteria>
    - `grep -c '<aside' apps/web/src/components/Profile.tsx` returns `0`, and the file still contains the original card `className` string unchanged.
    - `apps/web/src/app/globals.css` contains `@property --profile-width` and `html[data-sidebar-right="collapsed"]`, both outside the `.dark { }` block, and contains no new `@theme inline` entry for either width property.
    - `SidebarShell.tsx` contains exactly one implementation of the per-side tri-state logic, invoked for both `"left"` and `"right"` — asserted by reading the file; no copy-pasted second state machine.
    - `SidebarToggleRight` is rendered inside the row's `flex items-center gap-2` div, after `<ThemeToggle />` in source order.
    - `grep -c 'SubscribeSection' apps/web/src/components/layout/SidebarShell.tsx` still returns `0`, and `grep -rn 'NEXT_PUBLIC_RESEND' apps/web/src` still returns no lines (the stop-ship guard is re-checked at every wave).
    - `npm run lint --prefix apps/web` and `npm run build --prefix apps/web` both succeed.
    - Observed and recorded: the four `<main>` widths 864 / 1064 / 1104 / 1304 px, one complementary region for the right panel, the ring identical across both themes and both states, and the icon fallback rendering on a deliberately broken avatar path.
  </acceptance_criteria>
  <done>Both sidebars collapse and expand independently, the right side's track animates to 0px, the right panel is a single landmark, and the subscribe form is still intact — committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reader's `localStorage` → `SidebarShell` client runtime | `readSidebarPref("right")` reads a visitor-writable store |
| `templates/default/Layout.tsx` (server) → `SidebarShell` (client) | The `RESEND_API_KEY` gate sits inside the tree crossing this boundary; this plan edits the client side of it |
| `/avatar.png` (fork-supplied static asset) → `SidebarToggleRight` | A fork controls this file; it may be absent, wrong-typed, or oversized |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-01 | Tampering | `readSidebarPref("right")` via `apps/web/src/lib/sidebar.ts` | medium | mitigate | Unchanged from plan 10-01: the right side uses the same strict allowlist parse, so a tampered `nolog:sidebar:right` value yields `null` (auto) rather than flowing into an attribute value. This plan adds a second caller, not a second parser. |
| T-10-02 | Information Disclosure | `SidebarShell.tsx` (client) ← `Layout.tsx` (server) | high | mitigate | This plan edits `SidebarShell.tsx`, the exact file where a careless `SubscribeSection` import would move a secret gate into client code. Both stop-ship greps are re-run in task 2's acceptance criteria: `SubscribeSection` count `0` in `SidebarShell.tsx`, and no publicly-prefixed Resend variable anywhere under `apps/web/src`. |
| T-10-04 | Denial of Service (local, cosmetic) | `SidebarToggleRight`'s `next/image` load of a fork-controlled `/avatar.png` | low | mitigate | A missing, malformed or non-image asset resolves through the native `onError` handler to the D-14 icon fallback, so the control remains operable and the row's geometry is unchanged. No retry loop is introduced, so a persistently failing asset cannot generate repeated requests. |
| T-10-SC | Tampering | npm / package-manager installs | low | accept | This phase installs nothing (D-07). `lucide-react` and `next/image` are pre-existing, already-approved dependencies; `10-RESEARCH.md`'s Package Legitimacy Audit records zero new packages, zero `[SUS]`, zero `[SLOP]`. |

## Non-applicable ASVS categories

| Category | Applies | Why not |
|---|---|---|
| V2 Authentication | no | No auth surface; the avatar toggle is explicitly forbidden from implying an account (see `must_haves.prohibitions`) |
| V3 Session Management | no | No cookie or session state |
| V4 Access Control | no | No access-control logic |
| V6 Cryptography | no | No cryptographic operation |
</threat_model>

<verification>
- `npm run lint --prefix apps/web` after each task commit.
- `npm run build --prefix apps/web` at the end of the plan.
- Task 2's `human-check` battery, driven with the gstack `/browse` skill and recorded with the real measured
  numbers. This project has no test framework and none may be added.
- The stop-ship greps are re-run in this wave even though plan 10-01 already passed them, because this plan
  edits `SidebarShell.tsx`.
- Deliberate residual after this plan: the collapsing panel is not yet `inert` and focus is not yet rescued.
  Plan 10-03 closes A11Y-02 and A11Y-03 together, in that order, because shipping `inert` without the focus
  rescue would strand focus inside an inert element — strictly worse than shipping neither.
</verification>

<success_criteria>
- The circular profile-image button collapses and expands the right sidebar (SIDE-02).
- Either side collapses without disturbing the other; both collapsed gives a 1304px content column at a
  1400px viewport (SIDE-03, SIDE-04).
- The accent ring is present and identical in both states and both themes, and the button reads as a
  control rather than an account menu (SIDE-09, D-12).
- The avatar toggle stays operable when the avatar asset is missing (D-14).
- Both toggles report `aria-expanded` and name their panel via `aria-controls` (A11Y-01).
- The avatar toggle's accessible name is action-phrased, matches its `title`, and is distinct from the
  Profile card avatar's alt text (A11Y-05).
- `Profile.tsx` renders no landmark; the right panel is one complementary region.
- The subscribe form still renders and no publicly-prefixed Resend variable exists (SIDE-10 re-checked).
</success_criteria>

<output>
Create `.planning/phases/10-collapsible-sidebars-reading-width/10-02-SUMMARY.md` when done.
Record, verbatim: the four measured `<main>` widths, the accessibility-tree region count for the right
panel, whether the icon fallback was exercised against a real 404 (and that the config change was reverted),
and whether the subscribe form was observed rendering with real Resend vars or correctly absent without them.
</output>
