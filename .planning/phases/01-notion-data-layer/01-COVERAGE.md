No external API integration: this phase extends the already-integrated `NologClient` (`packages/core/src/client.ts`) with two new methods (`getUnemailedPublicPosts()` query-filter read and `markEmailed()` checkbox write, plus a private `patchPage()`) against the Notion REST API surface the codebase already calls via `queryDatabase()`/`getPost()`. No new service, SDK, or external API is introduced — the deterministic API-coverage detector returned `detected: false`, and the phase scope is a pure single-file-family extension (RESEARCH.md: "no new package, no new external dependency, no new architectural layer").
