---
phase: 08-content-rendering-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/lib/notion-x.ts
  - apps/web/src/app/post/[id]/page.tsx
  - apps/web/src/templates/default/PostPage.tsx
  - apps/web/src/app/api/diagnose-page/route.ts
autonomous: true
requirements: [CONT-03, CONT-05]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The outbound request notion-client makes to Notion's unofficial endpoint carries a User-Agent header naming NoLog and a contact URL, sourced from one exported module constant, applied only to the notion-client construction (D-01/D-02/D-03/D-06)."
    - "The fetch-failed state and the arrived-but-empty state never render the same sentence, and at the emptiness heuristic's exact boundary the branch taken is deterministic and governed by an explicitly stated threshold rule, not by accident."
    - "A null recordMap (fetch threw) and a non-null recordMap with nothing renderable produce two different specified outputs; neither produces a crash, and neither produces a content area with no message at all."
    - "Repeated requests for the same post render the same state — the branch is a function of the fetch outcome alone, not of request order, cache state, or which ISR regeneration served it."
    - "Each of the two states renders exactly one sentence; neither state can render zero sentences, leaving a silently blank content area."
    - "When getPageRecordMap throws (caught), the reader sees \"This post's content could not be loaded right now.\" — never the no-content sentence."
    - "When getPageRecordMap succeeds but the returned recordMap has no renderable content blocks, the reader sees \"This post has no content yet.\" — never the fetch-failed sentence, and never the pre-Phase-8 combined string."
    - "Both sentences render inside the existing .notion-content-wrapper and its surrounding article element with no fixed height and no new width constraint — text wraps naturally at every viewport exactly as the single sentence does today; no truncation or clipping is introduced."
    - "isDiagnosticsEnabled() still exists and is still exported from apps/web/src/lib/notion-x.ts, and apps/web/src/lib/post-availability.ts compiles with zero modifications (D-13)."
    - "The leg-name prefixes [PostPage:recordMap] and [PostPage:chrome] still appear in apps/web/src/app/post/[id]/page.tsx's catch blocks after the teardown — CONT-01 survives the removal of the diagnostic payload."
    - "The post still renders through notion-client + react-notion-x: apps/web/package.json's dependency list is byte-identical to its pre-phase state (D-01/D-07)."
    - "apps/web/src/templates/terminal/PostPage.tsx is unmodified and still compiles — its prop interface is structurally separate and never receives the new prop."
  artifacts:
    - path: "apps/web/src/lib/notion-x.ts"
      provides: "The User-Agent fix (D-01/D-02) plus the emptiness predicate CONT-05 branches on; retains isDiagnosticsEnabled for post-availability.ts"
      exports: ["NOLOG_USER_AGENT", "getPageRecordMap", "isRecordMapEmpty", "isDiagnosticsEnabled"]
      contains: "ofetchOptions"
    - path: "apps/web/src/app/post/[id]/page.tsx"
      provides: "contentFetchFailed computed at the content leg's catch and threaded to the default template; plain bracket-prefixed error logging"
      contains: "contentFetchFailed"
    - path: "apps/web/src/templates/default/PostPage.tsx"
      provides: "The CONT-05 three-way content branch rendering the two locked sentences"
      contains: "This post has no content yet."
  key_links:
    - from: "apps/web/src/lib/notion-x.ts"
      to: "notion-client NotionAPI constructor"
      via: "ofetchOptions.headers carries NOLOG_USER_AGENT onto every request the client makes"
      pattern: "ofetchOptions"
    - from: "apps/web/src/app/post/[id]/page.tsx"
      to: "apps/web/src/templates/default/PostPage.tsx"
      via: "passes contentFetchFailed alongside recordMap at both DefaultPostPage call sites"
      pattern: "contentFetchFailed={contentFetchFailed}"
    - from: "apps/web/src/templates/default/PostPage.tsx"
      to: "apps/web/src/lib/notion-x.ts"
      via: "imports isRecordMapEmpty to discriminate the arrived-but-empty branch"
      pattern: "isRecordMapEmpty"
    - from: "apps/web/src/lib/post-availability.ts"
      to: "apps/web/src/lib/notion-x.ts"
      via: "pre-existing import of isDiagnosticsEnabled that the teardown must not break"
      pattern: "isDiagnosticsEnabled"
  prohibitions:
    - requirement_id: CONT-03
      category: transparency
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT present the outbound client as a web browser or as any third party. The User-Agent must name this software and carry a contact URL so a Notion-side operator can attribute and reach the client. Evading a block by impersonation is forbidden even where impersonation would also return 200 — the honest string was measured to work, so there is no efficacy argument for the dishonest one."
    - requirement_id: CONT-05
      category: transparency
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT tell a reader that a post has no content when the truth is that the site failed to fetch it. The no-content sentence must never serve as the fallback for an unknown, errored, or unclassified outcome — doing so silently misattributes a site failure to the author having written nothing."
---

<objective>
Ship, in one commit series and with no push, the three code changes Phase 8 delivers: the `User-Agent` fix that Phase 7's evidence identified as the root cause, the CONT-05 split of one fallback sentence into two distinct reader-facing states, and the complete D-19 teardown of Phase 7's diagnosis-only surfaces.

Purpose: `07-EVIDENCE.md` established (HIGH confidence, five candidates eliminated, single-variable experiment) that Cloudflare answers Node's default `user-agent: node` with 403 + an HTML challenge page in front of the `loadPageChunk` endpoint that serves `getPageRecordMap`. That is why no post body renders. The fix is a header. Because every deploy invalidates the entire ISR cache and Phase 9 needs an uninterrupted >1h idle window, D-14 requires the fix, CONT-05 and the teardown to reach production in **one** deploy — so all three land here, and nothing is pushed until plan 08-03.

Output: an exported `NOLOG_USER_AGENT` constant wired through `NotionAPI`'s `ofetchOptions`, an exported `isRecordMapEmpty` predicate, a `contentFetchFailed` signal threaded from the route to the default template's new three-way content branch, and the deletion of `apps/web/src/app/api/diagnose-page/route.ts` plus every diagnosis-only helper in `lib/notion-x.ts`.
</objective>

<execution_context>
@/home/alpha-pi/.claude/gsd-core/workflows/execute-plan.md
@/home/alpha-pi/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-content-rendering-fix/08-CONTEXT.md
@.planning/phases/08-content-rendering-fix/08-RESEARCH.md
@.planning/phases/08-content-rendering-fix/08-PATTERNS.md
@.planning/phases/08-content-rendering-fix/08-UI-SPEC.md
@.planning/phases/07-content-failure-isolation-live-diagnosis/07-EVIDENCE.md
@.planning/research/PITFALLS.md
@apps/web/src/lib/notion-x.ts
@apps/web/src/app/post/[id]/page.tsx
@apps/web/src/templates/default/PostPage.tsx
@apps/web/src/lib/post-availability.ts
</context>

<!-- planner-discipline-allow: describeFetchFailure -->
<!-- planner-discipline-allow: diagnose-page -->
<!-- planner-discipline-allow: Content could not be loaded -->
<!-- planner-discipline-allow: NOTION_DEBUG_ROUTE_SECRET -->
<!-- planner-discipline-allow: isFetchErrorShape -->
<!-- planner-discipline-allow: describePageIdShape -->
<!-- planner-discipline-allow: LOAD_PAGE_CHUNK_URL -->

## Names resolved by this plan (Claude's Discretion per 08-CONTEXT.md)

| Discretion item | Resolved value | Why |
|---|---|---|
| The exact UA string within D-03's shape | `NoLog (+https://github.com/4lph4-dvlp/NoLog)` | Software name plus a contact URL, no version (D-04). Measured to return 200 against the live endpoint (`07-EVIDENCE.md`). |
| Module the shared constant lives in | `apps/web/src/lib/notion-x.ts`, exported as `NOLOG_USER_AGENT` | The one module that owns the `notion-client` construction. Exported (unlike the file-local `DIAGNOSTICS_GATE_VALUE`) because Phase 9's thumbnail-proxy work reuses it (D-06). |
| Exact CONT-05 wording within D-11's shape | Locked by `08-UI-SPEC.md`: `This post has no content yet.` / `This post's content could not be loaded right now.` | Already through UI-checker sign-off (6/6). Not the executor's to re-word. |
| Emptiness helper name + module | `isRecordMapEmpty(recordMap)`, exported from `apps/web/src/lib/notion-x.ts` | Matches `08-PATTERNS.md`'s recommended placement and `08-UI-SPEC.md`'s illustrative markup, so the template imports it directly. `templates/default/PostPage.tsx` is a Server Component and `post/[id]/page.tsx` already pulls `notion-x` into that module graph, so the import adds no new client-side surface. |
| Emptiness threshold expression | A named module constant `RENDERABLE_BLOCK_MIN = 2`, with `isRecordMapEmpty` returning `Object.keys(recordMap.block ?? {}).length < RENDERABLE_BLOCK_MIN` | Truth 2 requires the boundary be "governed by an explicitly stated threshold rule, not by accident". A named scalar also gives plan 08-02 exactly one value to recalibrate against a real empty Notion page. |
| `terminal` template parity | Not changed | Out of scope this milestone (TMPL-F01). `08-RESEARCH.md` Finding 3 confirms `TerminalPostPageProps` is a structurally separate interface, so it compiles unchanged with no defensive code. |
| Commit shape within the single deploy | Three commits, one per task, all on `main`, **none pushed** | D-14 requires one deploy, not one commit. The push is plan 08-03's first task. |

## Artifacts this phase produces (plan 08-01 share)

**Created — symbols**
- `NOLOG_USER_AGENT: string` (exported) — `apps/web/src/lib/notion-x.ts`
- `RENDERABLE_BLOCK_MIN: number` (module-local) — `apps/web/src/lib/notion-x.ts`
- `isRecordMapEmpty(recordMap: ExtendedRecordMap): boolean` (exported) — `apps/web/src/lib/notion-x.ts`
- `contentFetchFailed: boolean` — local binding in `apps/web/src/app/post/[id]/page.tsx`, and a new **required** prop on `DefaultPostPageProps`
- `ofetchOptions.headers["User-Agent"]` — new option on the existing `NotionAPI` construction

**Created — reader-facing strings**
- `This post has no content yet.` — `apps/web/src/templates/default/PostPage.tsx`
- `This post's content could not be loaded right now.` — `apps/web/src/templates/default/PostPage.tsx`

**Removed — files**
- `apps/web/src/app/api/diagnose-page/route.ts` (82 lines, in full) — the route path it registered disappears from the `next build` route list

**Removed — symbols** (all in `apps/web/src/lib/notion-x.ts`)
- `describeFetchFailure()` (lines 86-171), including its D-04 raw-fetch probe
- `isFetchErrorShape()` (lines 47-51)
- `describePageIdShape()` (lines 54-62)
- `LOAD_PAGE_CHUNK_URL` (line 68)
- `BODY_EXCERPT_MAX_LENGTH` (line 71) — **this file's copy only**; `post-availability.ts:37` has its own independent same-named constant that stays
- the `parsePageId` import (line 2) — its only use is inside the deleted function, so leaving it orphans an unused import and fails lint

**Removed — bindings and stale references** (`apps/web/src/app/post/[id]/page.tsx`)
- the deleted helper's name from the `@/lib/notion-x` import on line 2
- both call sites (lines 119, 142)
- the D-17 audit comment's bullet about that helper (around line 108) and the head-comment reference to the deleted route in `notion-x.ts:22`

**Removed — operator surface** (plan 08-03 confirms; no code here)
- Vercel Production env vars `NOTION_DEBUG_DIAGNOSTICS` and `NOTION_DEBUG_ROUTE_SECRET`

**Preserved — must survive unchanged**
- `isDiagnosticsEnabled()` + `DIAGNOSTICS_GATE_VALUE` in `notion-x.ts` (D-13)
- `apps/web/src/lib/post-availability.ts`, byte-for-byte
- `apps/web/src/templates/terminal/PostPage.tsx`, byte-for-byte
- the `[PostPage:recordMap]` / `[PostPage:chrome]` / `[PostPage:post]` prefixes, the per-concern catch decomposition, `classifyMissingPost`, `PostUnavailable`

<edge_coverage_accounting>
## Probe accounting (no silent drops)

The spec-less probe fallback surfaced **5** applicable edges: 4 resolved (explicit), 1 resolved (backstop).

- Edges 1-4 (explicit) → `must_haves.truths` of **this plan**, as plain strings.
- Edge 5 (backstop, `CONT-05 / encoding`) → `must_haves.truths` of **plan 08-02**, as a structured `{ statement, verification: backstop }` marker — that is the plan whose task actually exercises it against a real empty Notion page, and it must abstain to `human_needed` rather than silently pass if that check is not run.
- Total across the plan set: 4 explicit + 1 backstop = 5 surfaced. Zero unresolved, zero dropped.

`08-UI-SPEC.md`'s `## UI Considerations` contributed **3** resolved (explicit) rows; all three are in this plan's `truths`. Its 6 dismissed rows carry their own reasons in the UI-SPEC and are correctly not lifted.

Prohibition recall kept **2** bespoke items (both in this plan's `must_haves.prohibitions`, descriptor-less). Two canon candidates were dropped with breadcrumbs rather than minted:
- *Reader-facing error messages must not leak internals* — canon information-disclosure; covered by `/gsd-secure-phase` and by `T-08-02` in this plan's threat model; not minted here.
- *`NOTION_TOKEN_V2` / secret handling on the outbound path* — canon secrets management; covered by `/gsd-secure-phase`; not minted here.
</edge_coverage_accounting>

<assumption_delta_decision>
**Checkpoint:** assumption-delta (advisory, non-blocking).
**Signal:** one `pluralization` hit on the word **"multiple"** in ROADMAP Phase 8 SC#1 — *"verified across multiple posts and repeated over several requests"*.
**Decision:** `no-change`.
**Rationale:** "multiple" here sets the **breadth of the verification sample** (how many posts an operator must check before signing off), not a singular→plural change in any data shape, type, prop, or storage identity — nothing in this phase moves from one-of-X to many-of-X. A prose false positive; the sampling requirement it describes is already carried as a first-class acceptance criterion in plan 08-03.
</assumption_delta_decision>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end "the post body comes back" — one honest User-Agent through the one client (D-01/D-02/D-03/D-04/D-05/D-06)</name>
  <read_first>
    - `apps/web/src/lib/notion-x.ts` (full file, 172 lines — you are editing lines 1-16 and must not disturb lines 25-38)
    - `.planning/phases/07-content-failure-isolation-live-diagnosis/07-EVIDENCE.md` § "Hand-off to Phase 8" — the six established facts. Do NOT re-derive the root cause.
    - `.planning/phases/08-content-rendering-fix/08-PATTERNS.md` § `apps/web/src/lib/notion-x.ts` — the exact constructor shape and the exported-constant precedent
    - `apps/web/src/lib/notion.ts` lines 1-10 — the `NOTION_CACHE_TAG` module-constant convention this follows
  </read_first>
  <files>apps/web/src/lib/notion-x.ts</files>
  <action>
    Add one exported module-level constant to `apps/web/src/lib/notion-x.ts`, declared above the existing `NotionAPI` construction:
    `NOLOG_USER_AGENT`, `UPPER_SNAKE_CASE`, value exactly `NoLog (+https://github.com/4lph4-dvlp/NoLog)`. No version number (D-04). No `process.env` read, no `site.config.ts` field, no default-and-override — the value is hardcoded, and that is load-bearing, because D-19's premise (and the operator's condition for having accepted Phase 7's instrumentation at all) is that a forker ends up with **zero** net new env vars (D-05). Carry a short comment saying why it is hardcoded rather than configurable, in the style of the existing gate-value comment at lines 29-32.

    Then pass it to the existing `new NotionAPI({...})` call by adding an `ofetchOptions` property whose `headers` object sets the key `User-Agent` to `NOLOG_USER_AGENT`. Keep the existing `authToken: process.env.NOTION_TOKEN_V2 || undefined` line unchanged and in place. This is the only construction that changes: per D-06, do not touch the official `@notionhq/client` in `lib/notion.ts`, the Resend client, the OG route, or any other outbound path — none of them has an observed problem, and a shared constant exists so Phase 9 has one place to reuse, not so this phase can rewrite working code.

    Mechanism, already verified by direct package inspection (`08-RESEARCH.md` D-02, `node_modules/notion-client/build/index.d.ts:34,40` types the constructor's `ofetchOptions?: FetchOptions`, and `build/index.js:538-545` merges `...this._ofetchOptions?.headers` into every request before only `Content-Type`, `cookie` and `x-notion-active-user-header` are applied). No patch, no `fetch` wrapper, no new dependency — D-07 forbids one and none is needed.

    Do NOT add any escalation defence, retry, backoff, or new detection logging for a future re-block (D-07/D-08): behaviour on that day is already safe because Phase 7 shipped the non-throwing degradation and the leg-named logs, and building for an unobserved failure mode is `PITFALLS.md` Pitfall 6's unverified safety net.

    Commit this task on its own. Do NOT push — the single deploy D-14 mandates is triggered by plan 08-03's first task.
  </action>
  <acceptance_criteria>
    - `grep -c 'export const NOLOG_USER_AGENT' apps/web/src/lib/notion-x.ts` returns `1`.
    - `grep -c 'NoLog (+https://github.com/4lph4-dvlp/NoLog)' apps/web/src/lib/notion-x.ts` returns `1` — the string appears exactly once, in the constant's declaration, and is not duplicated at the call site.
    - The UA string contains no digits-and-dots version token: `grep -Eq 'NOLOG_USER_AGENT = "NoLog/[0-9]' apps/web/src/lib/notion-x.ts` finds nothing (D-04).
    - No env var was introduced for it: `grep -c 'NOTION_USER_AGENT\|NEXT_PUBLIC_USER_AGENT' apps/web/src` returns `0` (D-05).
    - `grep -c 'ofetchOptions' apps/web/src/lib/notion-x.ts` returns `1`, and the block sets the header key `User-Agent` to `NOLOG_USER_AGENT` rather than to an inline literal.
    - Blast radius held: `git diff --stat` for this commit lists `apps/web/src/lib/notion-x.ts` and nothing else. In particular `apps/web/src/lib/notion.ts` is untouched (D-06).
    - No new dependency: `git diff --exit-code apps/web/package.json package-lock.json` exits `0` (D-07/SC#4).
    - `npm run build --workspace=apps/web` exits `0` and `npm run lint --workspace=apps/web` reports no new error in `notion-x.ts`. (Known pre-existing baseline: `apps/web/src/templates/terminal/components/TerminalConsole.tsx` already fails lint on `main` — see STATE.md Phase 7 Plan 01. Judge "no new error" against that baseline, do not attempt to fix it.)
  </acceptance_criteria>
  <verify>
    <automated>grep -c 'export const NOLOG_USER_AGENT' apps/web/src/lib/notion-x.ts; grep -c 'ofetchOptions' apps/web/src/lib/notion-x.ts; git diff --exit-code apps/web/package.json package-lock.json; npm run build --workspace=apps/web</automated>
    <automated>node -e 'const fs=require("fs");const m=fs.readFileSync("apps/web/src/lib/notion-x.ts","utf8").match(/NOLOG_USER_AGENT\s*=\s*"([^"]+)"/);if(!m){console.log("NO_CONSTANT");process.exit(1)}const id=process.env.UAT_PAGE_ID;if(!id){console.log("SKIPPED_NO_UAT_PAGE_ID");process.exit(0)}fetch("https://www.notion.so/api/v3/loadPageChunk",{method:"POST",headers:{"Content-Type":"application/json","User-Agent":m[1]},body:JSON.stringify({pageId:id,limit:30,chunkNumber:0,cursor:{stack:[]},verticalColumns:false})}).then(async r=>{const t=await r.text();const ok=r.status===200&&t.includes("\"block\"");console.log("status",r.status,"hasBlockMap",t.includes("\"block\""));process.exit(ok?0:1)}).catch(e=>{console.log("UNREACHABLE",e.message);process.exit(0)})'</automated>
    This second command is the tracer's genuine end-to-end proof: it reads the constant **out of the shipped source** (so it cannot drift from what deploys), issues the real request `notion-client` makes, and asserts a `200` carrying a recordMap-shaped body — the same single-variable experiment `07-EVIDENCE.md` used, re-run as a regression gate. Set `UAT_PAGE_ID` to any real public post id from the operator's Notion database. If it prints `SKIPPED_NO_UAT_PAGE_ID` or `UNREACHABLE`, record that verbatim in the SUMMARY as **not run** — do NOT record it as a pass, and do not treat it as a failure of the fix. A printed `status 403` **is** a real failure and must halt the plan. Either way this is not CONT-03 sign-off: per PITFALLS 12, CONT-03 is deployed-site-only and lands in plan 08-03.
  </verify>
  <done>`apps/web/src/lib/notion-x.ts` exports `NOLOG_USER_AGENT` and passes it as the `User-Agent` header through `NotionAPI`'s `ofetchOptions`; the build is green; no other file, dependency, or env var moved; the change is committed and unpushed.</done>
</task>

<task type="auto">
  <name>Task 2: D-19 teardown — delete the diagnosis-only surfaces, keeping the three things that must survive (D-13)</name>
  <reversibility rating="costly">Undoing this is not a one-line revert: it means restoring ~130 deleted lines plus an 82-line route file and re-adding two Vercel Production env vars (08-CONTEXT D-14). Accepted because the fix in Task 1 was measured working against the live endpoint before it shipped, and because deferring the teardown costs a second deploy that resets Phase 9's >1h idle window.</reversibility>
  <read_first>
    - `apps/web/src/lib/notion-x.ts` (full file — you are deleting lines 40-171 and the line-2 import, and keeping lines 1, 4-38)
    - `apps/web/src/app/post/[id]/page.tsx` lines 1-11 and 105-145 — the import line, the D-17 audit comment block, and both catch blocks
    - `apps/web/src/lib/post-availability.ts` lines 1-2, 37, 110-120 — **read-only**. This file must not be modified. It is why the predicate survives, and it has its own independent same-named excerpt-length constant that is NOT the one being deleted.
    - `apps/web/src/app/api/diagnose-page/route.ts` — read once before deleting, to confirm nothing in it is imported elsewhere
    - `.planning/phases/08-content-rendering-fix/08-RESEARCH.md` Finding 5 — the full three-file, five-reference enumeration
    - `.claude/CLAUDE.md` § Error Handling / Logging — the bracket-prefix + bare-error-object convention the replacement log lines follow
  </read_first>
  <files>apps/web/src/app/api/diagnose-page/route.ts (deleted), apps/web/src/lib/notion-x.ts, apps/web/src/app/post/[id]/page.tsx</files>
  <action>
    Three deletions and one substitution, in this order.

    **(a) Delete the route file in full.** Remove `apps/web/src/app/api/diagnose-page/route.ts` with `git rm`, and ensure no empty directory is left behind on disk. Everything inside it (its local secret-compare helper, its one-shot rejection latch, its imports) dies with the file; nothing else in the repo imports from it.

    **(b) Delete the diagnosis-only block from `apps/web/src/lib/notion-x.ts`.** Remove the exported failure-describing helper (lines 86-171 by current numbering, including its D-04 raw-fetch probe), and the four module-private things used only by it: the `FetchError`-shape type guard (47-51), the page-id-shape describer (54-62), the unofficial-endpoint URL constant (68), and this file's body-excerpt length constant (71). Then remove the now-orphaned `parsePageId` import on line 2 — its sole use was inside the deleted probe, and leaving it fails lint as an unused import. Finally, edit the surviving head comment on `getPageRecordMap` (lines 18-24) which names the deleted route as a call site: it now has exactly one call site, `post/[id]/page.tsx`, and the comment must say so rather than describe a file that no longer exists.

    **KEEP, and do not reorder out of exported scope:** `getPageRecordMap`, the diagnostics gate value constant, and the `isDiagnosticsEnabled` predicate (lines 25-38). This is D-13 resolution (a), and it is not stylistic: `apps/web/src/lib/post-availability.ts` — a permanent file this phase does not otherwise touch — imports the predicate at line 2 and calls it at line 116 inside its response-detail builder. Deleting it breaks the build. Do NOT "clean up" that file's own same-named body-excerpt constant at its line 37 either; it is a separate module-scope declaration with no import tying it to the one you are deleting.

    **(c) Replace both call sites in `apps/web/src/app/post/[id]/page.tsx`.** These are **not** inside the deleted route — they are in a permanent file, at lines 119 and 142, and a "delete the route and its helper" pass that misses them breaks the build. Remove the deleted helper's name from the `@/lib/notion-x` import on line 2, leaving `getPageRecordMap` imported. Then rewrite each `console.error` so it keeps its bracketed leg-name prefix as a plain first argument and passes the caught error object as the **second** argument rather than interpolating a stringified payload: the content-leg line keeps `[PostPage:recordMap]`, the chrome-leg line keeps `[PostPage:chrome]`. Both calls become synchronous — drop the `await` from them. CONT-01 requires the leg-name prefix survive; it does not require the JSON-shaped diagnostic payload survive. Object-form logging is `.claude/CLAUDE.md`'s own documented convention ("log error objects directly, not just strings, to preserve stack traces"). Also update the D-17 audit comment block around line 108, whose last bullet describes awaiting the now-deleted helper inside each catch — that bullet is now false and must be removed or rewritten to describe the plain logging call.

    Do not add replacement diagnostics of any kind (D-08). Adding fresh diagnostic code in the same commit that removes Phase 7's diagnostic code would be self-cancelling.

    The teardown must be visible in the diff as deletions, not as newly-dead code left in place. Commit this task on its own. Do NOT push.
  </action>
  <acceptance_criteria>
    - **Route gone:** `test ! -e apps/web/src/app/api/diagnose-page` succeeds, and `npm run build --workspace=apps/web`'s printed route list contains no `/api/diagnose-page` entry (this is the mechanically-checkable proxy for "the teardown actually happened").
    - **Helper gone, comment-immune:** `grep -rn --include="*.ts" --include="*.tsx" 'describeFetchFailure' apps/web/src | grep -v ':[[:space:]]*//' | grep -v ':[[:space:]]*\*' | wc -l` returns `0`. Every private helper is gone too: the same comment-stripped grep for `isFetchErrorShape`, for `describePageIdShape`, and for `LOAD_PAGE_CHUNK_URL` each returns `0`.
    - **No stale route references:** `grep -rn --include="*.ts" --include="*.tsx" 'diagnose-page' apps/web/src | wc -l` returns `0` — this catches the head comment in `notion-x.ts:22` that a deletion-only pass would leave behind.
    - **Secret env var gone from code:** `grep -rc 'NOTION_DEBUG_ROUTE_SECRET' apps/web/src` finds no matching file (`grep -rl` returns empty).
    - **COMPILE-BREAKING COUPLING #1 — the predicate survives.** `grep -c 'export function isDiagnosticsEnabled' apps/web/src/lib/notion-x.ts` returns `1`, `grep -c 'NOTION_DEBUG_DIAGNOSTICS' apps/web/src/lib/notion-x.ts` returns `1`, and `git diff --exit-code apps/web/src/lib/post-availability.ts` exits `0` (that file is unmodified, including its own line-37 constant).
    - **COMPILE-BREAKING COUPLING #2 — both permanent-file call sites replaced, not merely un-imported.** `grep -c 'PostPage:recordMap' 'apps/web/src/app/post/[id]/page.tsx'` returns `1` and `grep -c 'PostPage:chrome' 'apps/web/src/app/post/[id]/page.tsx'` returns `1` — the prefixes survive (CONT-01) while the comment-stripped `describeFetchFailure` grep above is simultaneously `0`. Both conditions must hold together; either alone is the failure mode.
    - **COMPILE-BREAKING COUPLING #3 — orphaned import removed.** `grep -c 'parsePageId' apps/web/src/lib/notion-x.ts` returns `0`, and `npm run lint --workspace=apps/web` reports no unused-import error for that file.
    - **The build is the real gate:** `npm run build --workspace=apps/web` exits `0`. This is what proves all three couplings were handled.
    - The diff for this commit is deletions plus the four edited lines/comments — no new function, no new constant, no replacement diagnostic surface (D-08).
  </acceptance_criteria>
  <verify>
    <automated>test ! -e apps/web/src/app/api/diagnose-page &amp;&amp; echo ROUTE_DIR_GONE; for s in describeFetchFailure isFetchErrorShape describePageIdShape LOAD_PAGE_CHUNK_URL diagnose-page; do printf '%s=' "$s"; grep -rn --include="*.ts" --include="*.tsx" "$s" apps/web/src | grep -v ':[[:space:]]*//' | grep -v ':[[:space:]]*\*' | wc -l; done; grep -c 'export function isDiagnosticsEnabled' apps/web/src/lib/notion-x.ts; grep -c 'parsePageId' apps/web/src/lib/notion-x.ts; grep -c 'PostPage:recordMap' 'apps/web/src/app/post/[id]/page.tsx'; grep -c 'PostPage:chrome' 'apps/web/src/app/post/[id]/page.tsx'; git diff --exit-code apps/web/src/lib/post-availability.ts; npm run build --workspace=apps/web</automated>
    Expected: `ROUTE_DIR_GONE`; the five symbol counts all `0`; `1` for the surviving predicate; `0` for the orphaned import; `1` and `1` for the two surviving leg prefixes; a clean exit on the untouched availability module; and a `0` exit from the build whose route list omits the deleted route.
  </verify>
  <done>The diagnostic route file and every diagnosis-only helper are deleted; the surviving predicate, its gate constant, and both leg-name prefixes are intact; the orphaned import and the two stale comments are gone; `post-availability.ts` and the build are both untouched-and-green; committed and unpushed.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: CONT-05 — split one fallback sentence into two distinct reader-facing states (D-10/D-11/D-12)</name>
  <precondition>`apps/web/src/lib/notion-x.ts` no longer exports the deleted failure-describing helper and `npm run build --workspace=apps/web` exits 0 — i.e. Task 2's teardown has landed, so this task edits the final-shape catch block rather than a block that is about to be rewritten underneath it.</precondition>
  <read_first>
    - `.planning/phases/08-content-rendering-fix/08-UI-SPEC.md` — the **binding** contract. § Copywriting Contract for the two locked sentences; § Placement &amp; Composition Notes for where they render; § UI Considerations for the three resolved acceptance criteria already lifted into this plan's `truths`.
    - `apps/web/src/templates/default/PostPage.tsx` lines 1-14 and 95-103 — the current prop interface and the exact two-way ternary being replaced
    - `apps/web/src/app/post/[id]/page.tsx` lines 112-121 and 147-166 — the content leg's catch and both default-template call sites
    - `.planning/phases/08-content-rendering-fix/08-RESEARCH.md` Finding 3 and Finding 4 — the prop-shape change, and why an arrived recordMap always has at least the page's own block
    - `apps/web/src/templates/terminal/PostPage.tsx` lines 13-19 — **read-only**, to confirm for yourself that its prop interface is separate and needs no change
  </read_first>
  <files>apps/web/src/lib/notion-x.ts, apps/web/src/app/post/[id]/page.tsx, apps/web/src/templates/default/PostPage.tsx</files>
  <behavior>
    Three outcomes, one per branch, discriminated only by data the render already holds — no second Notion call (D-10, PITFALLS 4):
    - Fetch threw (caught) ⇒ `recordMap` is null and the failure flag is true ⇒ render the could-not-be-loaded sentence.
    - Fetch returned a recordMap carrying renderable content ⇒ render the Notion page as today.
    - Fetch returned a recordMap with nothing renderable ⇒ render the no-content sentence.
    - Boundary: exactly at the threshold, one branch is taken deterministically — the predicate is a pure function of the already-fetched object, so repeated requests for the same post render the same state regardless of order or which regeneration served them.
    - No fourth outcome exists: every path through the branch emits either the Notion renderer or exactly one sentence. A silently blank content area is not reachable.
  </behavior>
  <action>
    **(a) Add the emptiness predicate to `apps/web/src/lib/notion-x.ts`,** exported alongside `getPageRecordMap`: a module-local numeric constant `RENDERABLE_BLOCK_MIN` set to `2`, and an exported `isRecordMapEmpty(recordMap)` returning whether the count of keys in the record map's `block` object (nullish-coalesced to an empty object) is **below** that constant. Type the parameter `ExtendedRecordMap` imported as a type from `notion-types`, which is already a direct dependency of `apps/web` — adding no package (D-07). Naming the threshold as a constant rather than inlining `<= 1` is required: the boundary rule must be explicitly stated, and plan 08-02 recalibrates exactly this one scalar against a real empty Notion page.

    Carry a comment recording the two facts that justify the value and, honestly, its open half: `notion-client`'s `getPage()` throws before returning when `block` is entirely absent, so any record map reaching this predicate carries at least the page's own container block — one entry means no content children. And: the exact boundary is `[ASSUMED]` (`08-RESEARCH.md` Finding 4, Assumption A1) and has never been observed against a genuinely empty public Notion page; plan 08-02 closes that.

    **(b) Thread the failure signal in `apps/web/src/app/post/[id]/page.tsx`.** Declare a `contentFetchFailed` boolean initialised `false` next to the existing `recordMap` declaration, and set it to `true` inside the content leg's catch alongside the existing null assignment. Do not derive it from `recordMap`'s truthiness — that field is separately needed by the renderer when content does exist, and collapsing the two is exactly what CONT-05 exists to undo. Pass `contentFetchFailed` as a prop at **both** default-template call sites (the `CONFIG.template === "default"` branch and the trailing fallback return). Pass it at **neither** terminal call site.

    **(c) Replace the two-way branch in `apps/web/src/templates/default/PostPage.tsx`.** Widen `DefaultPostPageProps` with a required `contentFetchFailed: boolean`, and narrow `recordMap` from `any` to `ExtendedRecordMap | null` (type-imported from `notion-types`) — `getPageRecordMap` is typed to resolve to exactly that, so the narrowing is sound; if the build disagrees, report the exact compiler error rather than silently reverting to the loose type. Import the predicate from `@/lib/notion-x`. Inside the existing content wrapper div, replace the current two-way ternary with a three-way one: render the Notion renderer when a record map is present and not empty; otherwise, when the failure flag is set, render a paragraph reading `This post's content could not be loaded right now.`; otherwise render a paragraph reading `This post has no content yet.` Escape the apostrophe in the first sentence as JSX requires.

    Both paragraphs reuse the **exact** existing class list `text-text-secondary italic` and sit in the **exact** existing wrapper — no new token, no new spacing, no new colour, no icon, no link, no button. Order matters for honesty: the failure flag is checked **before** the no-content fallback, so an unknown or errored outcome can never fall through to the sentence that tells a reader the author wrote nothing (this plan's second prohibition). Do NOT add a recovery hint to the failure sentence (D-12) — against a systemic block, waiting does not help and the sentence would be false; `PostUnavailable.tsx` keeps its own "check back" copy because it describes a genuinely transient whole-post failure, and the two are deliberately not harmonized. Do NOT add a retry button, a refresh link, or any client-side re-fetch either (D-09): a reader hitting a returned block must see exactly what they see today — title and metadata rendering normally, the body area carrying the sharpened failure sentence — and a manual retry control is CONT-F01, deferred to v2, which against a systemic block would fail identically anyway.

    Nothing else in either file moves: header, thumbnail, comments, and the surrounding layout chrome are untouched, and `templates/terminal/PostPage.tsx` is not edited at all. Commit this task on its own. Do NOT push.
  </action>
  <acceptance_criteria>
    - **The old combined sentence is gone:** `grep -c 'Content could not be loaded' apps/web/src/templates/default/PostPage.tsx` returns `0`.
    - **Both new sentences are present exactly once each:** `grep -c 'This post has no content yet.' apps/web/src/templates/default/PostPage.tsx` returns `1`, and a grep for `content could not be loaded right now` in the same file returns `1`.
    - **Both reuse the existing treatment:** every paragraph rendering either sentence carries the class list `text-text-secondary italic`, and `git diff apps/web/src/templates/default/PostPage.tsx` shows no change to the wrapper div's classes, to the surrounding article element, or to any line outside the content branch and the prop interface.
    - **The predicate exists and the threshold is named, not inlined:** `grep -c 'export function isRecordMapEmpty' apps/web/src/lib/notion-x.ts` returns `1` and `grep -c 'RENDERABLE_BLOCK_MIN' apps/web/src/lib/notion-x.ts` returns `2` (the declaration plus its single use).
    - **No live cross-check was added (D-10 / PITFALLS 4):** the predicate's body contains no `await`, no `fetch`, and no call into `@/lib/notion` — `grep -c 'fetch(' apps/web/src/lib/notion-x.ts` returns `0`.
    - **The flag is threaded, not derived:** `grep -c 'contentFetchFailed' 'apps/web/src/app/post/[id]/page.tsx'` returns `4` (declaration, assignment in the catch, and one prop pass at each of the two default-template call sites).
    - **The terminal template is untouched:** `git diff --exit-code apps/web/src/templates/terminal/PostPage.tsx` exits `0`, and the build still compiles its separate prop interface.
    - **No fourth outcome:** reading the final ternary, every branch resolves to either the Notion renderer or exactly one paragraph element — there is no path that returns `null`, `false`, or an empty fragment into the content wrapper.
    - `npm run build --workspace=apps/web` exits `0` and `npm run lint --workspace=apps/web` reports no new error in the three edited files (judged against the known pre-existing terminal-console baseline).
    - No dependency moved: `git diff --exit-code apps/web/package.json package-lock.json` exits `0`.
  </acceptance_criteria>
  <verify>
    <automated>grep -c 'Content could not be loaded' apps/web/src/templates/default/PostPage.tsx; grep -c 'This post has no content yet.' apps/web/src/templates/default/PostPage.tsx; grep -c 'content could not be loaded right now' apps/web/src/templates/default/PostPage.tsx; grep -c 'export function isRecordMapEmpty' apps/web/src/lib/notion-x.ts; grep -c 'RENDERABLE_BLOCK_MIN' apps/web/src/lib/notion-x.ts; grep -c 'fetch(' apps/web/src/lib/notion-x.ts; grep -c 'contentFetchFailed' 'apps/web/src/app/post/[id]/page.tsx'; git diff --exit-code apps/web/src/templates/terminal/PostPage.tsx; git diff --exit-code apps/web/package.json package-lock.json; npm run build --workspace=apps/web; npm run lint --workspace=apps/web</automated>
    Expected: `0`, `1`, `1`, `1`, `2`, `0`, `4`, two clean `git diff` exits, a green build, and no new lint error. The first `0` and the two following `1`s together are the CONT-05 assertion: the combined sentence is gone and both replacements are present exactly once.
  </verify>
  <done>`apps/web/src/templates/default/PostPage.tsx` renders three distinct outcomes from one branch — the Notion renderer, the could-not-be-loaded sentence, and the no-content sentence — driven by `contentFetchFailed` and `isRecordMapEmpty`, with no new Notion call, no new token, and the terminal template untouched; committed and unpushed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| NoLog server (Vercel function / ISR regeneration) → Notion's unofficial `loadPageChunk` endpoint, via Cloudflare | Outbound request this plan changes. Everything it sends — including the new `User-Agent` — is visible to Cloudflare, Notion, and any observer of that leg. |
| Server render → reader's browser (HTML) | The two new sentences cross this boundary. Anything interpolated into them becomes public. |
| Public internet → `/api/diagnose-page` | A boundary this plan **removes** by deleting the route. |

This phase's security posture is substantially *subtractive*: it deletes a secret-gated public route together with its bearer-secret comparison, which removes attack surface rather than adding it. The threats below are sized accordingly (ASVS L1; block on `high`).

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-01 | Information Disclosure | `NOLOG_USER_AGENT` on every outbound `notion-client` request (`lib/notion-x.ts`) | low | accept | The header deliberately discloses that this deployment runs NoLog and links its public source repo. That is the point (D-03): an honest, self-identifying bot UA with a contact URL is the well-behaved posture, and the disclosed facts are already public for any fork of a public template. It carries no token, no database id, no operator identity, and no per-deployment value — the string is a compile-time constant identical across every fork (D-05). Accepted by design; the alternative (browser impersonation) is forbidden by this plan's first prohibition. |
| T-08-02 | Information Disclosure | The two new reader-facing sentences in `templates/default/PostPage.tsx` | medium | mitigate | The split creates a new place internal failure state could surface that the single previous sentence did not have. Mitigation: both sentences are hardcoded string literals with **zero interpolation** — no error message, HTTP status, upstream response body, page id, or stack trace may be rendered into either. All diagnostic detail stays server-side in the `console.error` calls Task 2 rewrites. Enforced by Task 3's "present exactly once each" greps plus the no-fourth-outcome criterion; a criterion that fails if a template literal is introduced. |
| T-08-03 | Spoofing / Elevation of Privilege | Deletion of `apps/web/src/app/api/diagnose-page/route.ts` | low | mitigate | Removing the route eliminates a publicly-reachable, bearer-secret-gated endpoint and its constant-time comparison — a net reduction. The residual risk is an *orphaned* reference: a surviving comment, env var, or doc that would mislead an operator into believing a gate still exists or a secret is still honoured. Mitigation: Task 2's zero-match greps for the route path and for the route-secret env var, plus the build-route-list check; plan 08-03 confirms the deployed route 404s and the two Production env vars are absent from Vercel. |
| T-08-04 | Tampering | Loss of the diagnostic surface while the reader-facing failure path stays live | low | accept | After teardown, an operator investigating a future re-block has only the ungated leg-named log lines. Accepted deliberately (D-07/D-08): behaviour on that day is already safe — nothing throws out of the render, the body degrades to a fallback sentence, and the leg-named line names the failing call. Building a replacement detection surface in the same commit that removes one is self-cancelling, and defending an unobserved failure mode is `PITFALLS.md` Pitfall 6. |
| T-08-SC | Tampering (supply chain) | npm install surface | low | accept | **The package-legitimacy gate does not fire for this plan: it contains no package-manager install task.** D-07 (locked in both `REQUIREMENTS.md` and `08-CONTEXT.md`) forbids new dependencies, `08-RESEARCH.md`'s Package Legitimacy Audit records "not applicable", and every type used (`ExtendedRecordMap` from `notion-types`) is an already-installed direct dependency. Enforced positively: `git diff --exit-code apps/web/package.json package-lock.json` is an acceptance criterion on Tasks 1 and 3. |
</threat_model>

<verification>
Run after all three tasks, from the repo root:

1. `npm run build --workspace=apps/web` exits `0`, and its printed route list contains **no** `/api/diagnose-page` entry.
2. `npm run lint --workspace=apps/web` reports no new error in `lib/notion-x.ts`, `app/post/[id]/page.tsx`, or `templates/default/PostPage.tsx`. The pre-existing `templates/terminal/components/TerminalConsole.tsx` failure on `main` is the known baseline (STATE.md, Phase 7 Plan 01) and is out of scope.
3. Teardown complete and comment-immune — all zero:
   `for s in describeFetchFailure isFetchErrorShape describePageIdShape LOAD_PAGE_CHUNK_URL diagnose-page NOTION_DEBUG_ROUTE_SECRET; do printf '%s=' "$s"; grep -rn --include="*.ts" --include="*.tsx" "$s" apps/web/src | grep -v ':[[:space:]]*//' | grep -v ':[[:space:]]*\*' | wc -l; done`
4. The three couplings held: `grep -c 'export function isDiagnosticsEnabled' apps/web/src/lib/notion-x.ts` is `1`; `git diff --exit-code apps/web/src/lib/post-availability.ts` exits `0`; `grep -c 'parsePageId' apps/web/src/lib/notion-x.ts` is `0`; both `PostPage:recordMap` and `PostPage:chrome` still appear once each in the route file.
5. No dependency or env var moved: `git diff --exit-code apps/web/package.json package-lock.json` exits `0`; `grep -rl 'NOTION_USER_AGENT' apps/web/src` returns empty.
6. **Nothing is pushed.** `git status -sb` shows `main` ahead of `origin/main` by exactly the commits this plan created, and `git log origin/main..HEAD --oneline` lists them. D-14's single deploy is plan 08-03's first task.

Not verified here, by design: CONT-03 itself. `next dev` and a local `next build` structurally cannot exercise the deployed ISR path (PITFALLS 12), and a single load proves nothing (PITFALLS 15). Sign-off lives in plan 08-03's three-request procedure.
</verification>

<success_criteria>
- `NOLOG_USER_AGENT` is exported from `lib/notion-x.ts` and reaches the wire via `ofetchOptions.headers`, applied to the `notion-client` construction and nothing else.
- The reader-facing fallback is two distinct sentences, matching `08-UI-SPEC.md`'s locked copy verbatim, discriminated by the fetch outcome plus a named-threshold emptiness predicate — with no additional Notion call.
- `/api/diagnose-page` and every diagnosis-only helper are gone from the source and from the build's route list, while `isDiagnosticsEnabled()`, `post-availability.ts`, both leg-name prefixes, `classifyMissingPost` and `PostUnavailable` are intact.
- `npm run build --workspace=apps/web` exits `0` — the single objective proof that all three compile-breaking couplings were handled.
- `apps/web/package.json` and `package-lock.json` are unchanged (D-01/D-07, ROADMAP SC#4).
- Three commits exist on `main` and none has been pushed.
</success_criteria>

<output>
Create `.planning/phases/08-content-rendering-fix/08-01-SUMMARY.md` when done.

Record explicitly in the SUMMARY:
- The exact `NOLOG_USER_AGENT` value that shipped.
- The `RENDERABLE_BLOCK_MIN` value that shipped, and a restatement that its boundary is still `[ASSUMED]` pending plan 08-02.
- Whether the tracer's outbound probe ran, was `SKIPPED_NO_UAT_PAGE_ID`, or was `UNREACHABLE` — verbatim, never softened into a pass.
- The three commit SHAs, and confirmation that `origin/main` has not moved.
</output>
