---
phase: 06-documentation
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - README.md
  - README_KR.md
autonomous: true
requirements: [DOCS-01]

must_haves:
  truths:
    - "A forker scanning only the top of either README — the architecture diagram, the services table, and the feature list — learns that this template can email subscribers a digest, and that the capability is optional (D-10)."
    - "The cron-triggered notify flow is represented as its own diagram subgraph parallel to Content Management / Application Layer / Visitors, not as another node inside the request-driven Application Layer, so a reader is not misled into thinking it is a render path (D-11)."
    - "Resend appears in both Core Services tables as a bold service name with a single-word role and a one-sentence purpose, matching the shape every existing row uses (D-10, 06-PATTERNS.md pattern 2)."
    - "The email feature appears in both Features lists as one `- **Bold lead phrase:** one sentence.` bullet carrying the same optional framing the existing Cusdis comments bullet uses (D-10, 06-PATTERNS.md pattern 3)."
    - "Both Mermaid blocks stay syntactically balanced after the edit — each file's diagram contains 4 `subgraph` openers and 4 matching `end` closers — so GitHub's native Markdown preview renders them rather than falling back to raw text."
    - "The English and Korean diagrams, service tables, and feature lists carry the same number of elements after the edit, preserving the 1:1 structural parity convention both files have held since the project's first commit."

  artifacts:
    - path: "README.md"
      provides: "English `Notifications` mermaid subgraph, `**Resend**` Core Services row, optional-email-digest Features bullet"
      contains: "subgraph \"Notifications\""
    - path: "README_KR.md"
      provides: "Korean `알림` mermaid subgraph, `**Resend**` 주요 서비스 row, optional-email-digest 주요 기능 bullet"
      contains: "subgraph \"알림\""

  key_links:
    - from: "README.md / README_KR.md `Notifications` subgraph"
      to: "the existing `N[Notion Database]` / `N[Notion 데이터베이스]` node"
      via: "cross-subgraph edge from the notify route back into the shared Notion node — the node is reused, never duplicated"
      pattern: "NR *-->\\|[^|]+\\| *N$"
    - from: "README.md / README_KR.md `Notifications` subgraph"
      to: "the new Email Notifications section written by plan 06-01"
      via: "the diagram names the same three actors (Vercel Cron, Resend, subscriber) the section's setup steps configure"
      pattern: "Resend"

  prohibitions:
    - "MUST NOT represent the email feature in the diagram, the Core Services table, or the Features list in a way that reads as a required part of the core fork → Notion → Vercel deploy path. This project's stated core value is that every optional feature stays inert until its env vars are explicitly set; a forker who reads the top of the README and concludes they must now create a Resend account to deploy a blog has been misled by the documentation into abandoning a template that would have worked for them. The Cusdis precedent — an `Optional comments` edge label, an `Optional embedded comment widget.` purpose cell, and a Features bullet leading with `Optional` — is the framing every new representation must match. [DOCS-01, category: product-values]"
---

## Phase Goal

**ROADMAP `**Goal:**` line, verbatim (not rewritten):** A forker can configure and safely enable the email feature using only README.md/README_KR.md, with none of the feature's known silent-failure traps left undocumented.

> MVP_MODE note: this goal is not in the canonical `As a … I want to … so that …` user-story format and has not been invented here. See 06-01-PLAN.md for the same note and the `/gsd mvp-phase 6` remediation path.

<objective>
Make the email feature discoverable at the top of both READMEs, where every other capability of this template is represented.

Purpose: plan 06-01 makes the feature *configurable* for a forker who already knows it exists. This plan makes it *findable*. Cusdis — the only other optional, env-gated feature — appears in the diagram, the Core Services table, and the Features list; leaving email out of all three while giving it a full setup section would read as an inconsistency to anyone comparing the two, and would leave the architecture diagram a pre-feature snapshot (D-10).

Output: a `Notifications` / `알림` mermaid subgraph, one `**Resend**` service row, and one optional-email-digest feature bullet — in both files. No source files change.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-documentation/06-CONTEXT.md
@.planning/phases/06-documentation/06-PATTERNS.md
@.planning/phases/06-documentation/06-01-SUMMARY.md
@README.md
@README_KR.md
</context>

## Tracer-first shape

This plan is the expansion slice that follows 06-01's tracer, in the MVP task-order's "new slice" position: 06-01 proved the documentation path end to end through both language surfaces; this plan adds a second, thinner slice (discoverability) across the same two surfaces. Both tasks touch both languages — splitting by file would reintroduce the horizontal layering the phase's tracer shape rejects. It runs in wave 2 rather than wave 1 solely because it modifies the same two files 06-01 modifies; there is no content dependency beyond needing the section 06-01 creates to exist so the new representations point at something real.

## Reversibility

`reversible`. D-10 and D-11 are presentation choices in two Markdown files; undoing either is a scoped edit in one commit with no migration, no published contract, and no dependent system parsing the diagram. No `<reversibility>` element and no `checkpoint:decision` gate is emitted.

<tasks>

<task type="auto">
  <name>Task 1: Notifications subgraph in both architecture diagrams</name>

  <precondition>`README.md` contains a `## Email Notifications (Optional)` heading and `README_KR.md` contains a `## 이메일 알림 (선택)` heading — plan 06-01's output. The diagram introduces actors whose setup lives in that section; adding it against a README with no such section would advertise an undocumented feature.</precondition>

  <files>README.md, README_KR.md</files>

  <read_first>
    - `README.md` lines 22-42 — the `## How It Works` prose and the existing mermaid block: three `subgraph "Title Case Name"` blocks, nodes as `Letter[Label]`, edges as `-->|Action phrase|`, 4-space indentation inside subgraphs, `end` on its own indented line.
    - `README_KR.md` lines 22-42 — the Korean diagram: subgraph titles and edge labels translated (`"콘텐츠 관리"`, `"애플리케이션 계층"`, `"방문자"`, `-->|배포|`), node labels using Korean nouns where the English uses English nouns (`N[Notion 데이터베이스]`), and product/proper nouns left untranslated (`Next.js`, `react-notion-x`, `Vercel`, `Cusdis`).
    - `.planning/phases/06-documentation/06-PATTERNS.md` — §"1. Diagram node pattern" for both files, including the note that this diagram already supports cross-subgraph edges so the existing Notion node is reused rather than duplicated.
    - `.planning/phases/06-documentation/06-RESEARCH.md` — §"Recommended Diagram Node/Edge Additions (D-11)", including the planner notes on edge-label style and the `"알림"` Korean subgraph-name candidate.
    - `.planning/phases/06-documentation/06-CONTEXT.md` — D-10, D-11, and the Claude's Discretion entry granting latitude over exact node/edge labels within the existing conventions.
  </read_first>

  <action>
Add one new subgraph to each file's existing mermaid block, positioned after the Application Layer subgraph and before the Visitors subgraph, so the reading order runs content → application → notifications → audience. In `README.md` name it `Notifications`; in `README_KR.md` name it `알림`, paralleling the existing `"콘텐츠 관리"` / `"애플리케이션 계층"` / `"방문자"` titles.

The subgraph carries exactly four edges wiring the cron path end to end: the cron trigger reaching the notify route, the notify route querying Notion for posts not yet emailed, the notify route handing the digest to Resend, and Resend delivering to a subscriber. Introduce four node identifiers not already used in the diagram — `CR` for Vercel Cron, `NR` for the notify route, `RS` for Resend, `SUB` for the subscriber — and route the Notion query edge into the diagram's **existing** `N` node rather than declaring a second Notion node; the diagram already resolves cross-subgraph references.

Label every edge with an action phrase in the `-->|Action phrase|` form the existing edges use (`-->|Deploy|`, `-->|Optional comments|`), and per the plan's prohibition make the optional nature of the path legible at the diagram level exactly the way the Cusdis edge does — the Cusdis edge label is what establishes that a node in this diagram can be an opt-in capability rather than a required hop.

In the Korean file translate the four edge labels and the two describable node labels (`Vercel Cron` and the notify route) following the existing Korean diagram's register, and leave `Resend` and `Vercel Cron`'s product noun untranslated per the file's own convention of not translating product names. Keep indentation at four spaces inside the subgraph and place `end` on its own line at the same indentation the existing subgraphs use.

Do not touch the three existing subgraphs, their nodes, or their edges. Do not add a node to the Application Layer subgraph — D-11 fixes the cron path as a separate, non-request-driven trigger path, and folding it in would misrepresent it as another render path.
  </action>

  <verify>
    <automated>
cd "$(git rev-parse --show-toplevel)"
# New subgraph exists, in the right file, with the right title.
grep -q 'subgraph "Notifications"' README.md
grep -q 'subgraph "알림"' README_KR.md
# Mermaid block stays balanced: 4 openers, 4 closers, in both files.
test "$(grep -c 'subgraph ' README.md)" = "4"
test "$(grep -cE '^ +end$' README.md)" = "4"
test "$(grep -c 'subgraph ' README_KR.md)" = "4"
test "$(grep -cE '^ +end$' README_KR.md)" = "4"
# Exactly four edges inside the new subgraph, in both files.
test "$(sed -n '/subgraph "Notifications"/,/^ *end$/p' README.md | grep -c -- '-->')" = "4"
test "$(sed -n '/subgraph "알림"/,/^ *end$/p' README_KR.md | grep -c -- '-->')" = "4"
# Ordering: notifications sits between the application layer and the visitors subgraph.
awk '/subgraph "Application Layer"/{a=NR} /subgraph "Notifications"/{b=NR} /subgraph "Visitors"/{c=NR} END{exit !(a&&b&&c&&a<b&&b<c)}' README.md
awk '/subgraph "애플리케이션 계층"/{a=NR} /subgraph "알림"/{b=NR} /subgraph "방문자"/{c=NR} END{exit !(a&&b&&c&&a<b&&b<c)}' README_KR.md
# The existing Notion node is reused, not duplicated: still exactly one declaration in each file.
test "$(grep -c 'N\[Notion Database\]' README.md)" = "1"
test "$(grep -c 'N\[Notion 데이터베이스\]' README_KR.md)" = "1"
    </automated>
    <human-check>Open both README files in GitHub's Markdown preview (or paste each mermaid block into a Mermaid live renderer). Confirm each diagram renders as a diagram rather than falling back to a raw code block, that the Notifications/알림 subgraph appears as a distinct box, and that the edge into the Notion node draws across subgraph boundaries without a duplicate Notion box appearing. This is the one check no grep can make — 06-VALIDATION.md records it as a per-wave manual gate.</human-check>
  </verify>

  <acceptance_criteria>
    - `grep -q 'subgraph "Notifications"' README.md` and `grep -q 'subgraph "알림"' README_KR.md` both succeed.
    - `grep -c 'subgraph ' ` returns `4` for each file (baseline before this task: `3`), and `grep -cE '^ +end$'` returns `4` for each file (baseline: `3`) — the block is syntactically balanced.
    - `sed -n '/subgraph "Notifications"/,/^ *end$/p' README.md | grep -c -- '-->'` returns `4`; the Korean equivalent returns `4`.
    - The awk ordering assertion passes in both files — the new subgraph's line number is strictly between the application-layer subgraph's and the visitors subgraph's.
    - `grep -c 'N\[Notion Database\]' README.md` returns `1` and `grep -c 'N\[Notion 데이터베이스\]' README_KR.md` returns `1` — the shared Notion node was reused via a cross-subgraph edge, not duplicated.
    - The existing Application Layer subgraph is unchanged: `sed -n '/subgraph "Application Layer"/,/^ *end$/p' README.md | grep -c -- '-->'` still returns `3`.
    - Human-check confirms both diagrams render without a Mermaid syntax error.
  </acceptance_criteria>

  <done>Both architecture diagrams show the cron → notify route → Resend → subscriber path as its own subgraph, reusing the shared Notion node, rendering cleanly, with the three pre-existing subgraphs untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Resend service row and optional email-digest feature bullet, both languages</name>

  <files>README.md, README_KR.md</files>

  <read_first>
    - `README.md` lines 44-63 — the `## Core Services` table (`| **ServiceName** | RoleWord | One-sentence purpose. |`, note the existing column alignment markers) and the `## Features` bullet list (`- **Bold lead phrase:** One-sentence elaboration.`), with the Cusdis row and the `Optional comments` bullet as the exact framing analogs.
    - `README_KR.md` lines 44-63 — the `## 주요 서비스` table (Korean role word, purpose sentence ending in `-니다`) and the `## 주요 기능` list, with the `**Cusdis** | 댓글 | 선택적으로 사용할 수 있는 임베드 댓글 위젯입니다.` row and the `- **선택 댓글:**` bullet as the analogs.
    - `.planning/phases/06-documentation/06-PATTERNS.md` — §"2. Core Services table row pattern" and §"3. Features bullet pattern" for both files.
    - `.planning/phases/06-documentation/06-CONTEXT.md` — D-10, and the §"Established Patterns" note that every optional feature in this repo gets one diagram element, one service row, and one feature bullet.
  </read_first>

  <action>
Append one row to each Core Services table, after the existing Cusdis row so the two optional services sit together at the end of the list. Use the table's exact row shape: the service name `Resend` bolded, a single-word role, and a purpose sentence of one line ending in a period. In `README.md` the role word is `Email`; in `README_KR.md` use the Korean equivalent and end the purpose sentence in `-니다` to match every other Korean row. Per the plan's prohibition, the purpose cell must carry the optional framing the Cusdis row's purpose cell carries — a reader comparing the two rows should see both marked the same way.

Append one bullet to the end of each Features list, using the list's `- **Bold lead phrase:** One-sentence elaboration.` shape. The lead phrase leads with the optional word exactly as the existing comments bullet does (`Optional comments` in English, `선택 댓글` in Korean), and the elaboration states in one sentence that subscribers get a daily digest of newly published posts by email. Do not describe the setup steps here — that content belongs to the section plan 06-01 wrote.

Preserve each table's existing column alignment characters and each list's existing ordering; do not reflow, re-sort, or re-align the pre-existing rows and bullets. The English and Korean files must end this task with equal row counts and equal bullet counts.
  </action>

  <verify>
    <automated>
cd "$(git rev-parse --show-toplevel)"
# The Resend row exists in both service tables.
grep -qE '^\| \*\*Resend\*\*' README.md
grep -qE '^\| \*\*Resend\*\*' README_KR.md
# Row count grew by exactly one in each file and the two files agree.
EN_ROWS=$(grep -cE '^\| \*\*' README.md); KR_ROWS=$(grep -cE '^\| \*\*' README_KR.md)
test "$EN_ROWS" = "6" && test "$KR_ROWS" = "6"
# Feature bullet count grew by exactly one in each file and the two files agree.
EN_FEAT=$(awk '/^## Features$/{f=1;next} /^## Vercel Deployment$/{f=0} f' README.md | grep -cE '^- \*\*')
KR_FEAT=$(awk '/^## 주요 기능$/{f=1;next} /^## Vercel 배포$/{f=0} f' README_KR.md | grep -cE '^- \*\*')
test "$EN_FEAT" = "9" && test "$KR_FEAT" = "9"
# Prohibition gate: the new representations carry the optional framing the Cusdis precedent uses.
grep -E '^\| \*\*Resend\*\*' README.md | grep -qi 'optional'
grep -E '^\| \*\*Resend\*\*' README_KR.md | grep -q '선택'
awk '/^## Features$/{f=1;next} /^## Vercel Deployment$/{f=0} f' README.md | grep -E '^- \*\*' | grep -i 'digest' | grep -qi 'optional'
awk '/^## 주요 기능$/{f=1;next} /^## Vercel 배포$/{f=0} f' README_KR.md | grep -E '^- \*\*' | grep -q '선택'
# Scope guard: exactly two files changed across the whole phase.
test "$(git diff --name-only HEAD -- README.md README_KR.md | wc -l)" = "2"
    </automated>
    <human-check>Read the Core Services table and Features list in both files as a first-time visitor would. Confirm the email feature reads as one more opt-in capability alongside Cusdis comments, not as a new prerequisite for deploying the blog — the core fork → Notion → Vercel story must be unchanged for a forker who never wants email.</human-check>
  </verify>

  <acceptance_criteria>
    - `grep -qE '^\| \*\*Resend\*\*' ` succeeds in both README.md and README_KR.md.
    - `grep -cE '^\| \*\*' ` returns `6` for each file (baseline before this task: `5`).
    - The Features bullet count is `9` in each file (baseline: `8`), measured within the features section region via the awk range shown in `<verify>`.
    - Prohibition gate passes: the `**Resend**` row's line matches `optional` case-insensitively in README.md and contains `선택` in README_KR.md; the new digest feature bullet matches `optional` in README.md and the new Korean bullet contains `선택`.
    - The pre-existing five service rows and eight feature bullets are unmodified — `git diff -- README.md` shows only additions in the table and list regions, no modified lines among the pre-existing rows.
    - `git diff --name-only HEAD -- README.md README_KR.md | wc -l` returns `2`, and `git diff --name-only HEAD | wc -l` returns `2` — no file outside the two READMEs was touched by this documentation-only phase.
  </acceptance_criteria>

  <done>Both READMEs represent the email feature in all three top-of-document surfaces — diagram, services table, features list — using the same optional framing Cusdis already carries, with every pre-existing row and bullet untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| working tree → public GitHub fork | README.md and README_KR.md are world-readable in this repo and in every fork; anything written here is published permanently, including in git history. |
| README diagram/table prose → forker's deploy decision | The top-of-document representation is what a prospective forker reads before deciding whether this template fits them. Misrepresenting an optional capability as required changes an external party's decision in a way this repo cannot later correct. |

This plan changes no code, no config, and no infrastructure — it appends one mermaid subgraph, one table row, and one list bullet per file. It introduces no new env var, no credential surface, and no example value.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-05 | Information Disclosure | New mermaid subgraph node and edge labels | low | mitigate | Node and edge labels name service roles only (`Vercel Cron`, `Notify Route`, `Resend`, `Subscriber`). No endpoint path carrying a secret, no audience ID, no domain belonging to a real forker is written into the diagram. Task 1's acceptance criteria fix the node identifiers, so no free-form value can leak in. |
| T-06-06 | Spoofing | Misrepresenting an optional feature as a required part of the stack | medium | mitigate | The `must_haves.prohibitions` item plus Task 2's automated framing gates (`Resend` row must match `optional` / `선택`, new bullet must match `optional` / `선택`) prevent the documentation from asserting a dependency the code does not have. The Cusdis row is the reference framing. |
| T-06-07 | Tampering | Mermaid block syntax corruption breaking the rendered diagram | low | mitigate | Balance gates assert 4 `subgraph` openers against 4 indented `end` closers in each file, plus a human render check via GitHub Markdown preview — a corrupted block degrades to raw text rather than rendering, which the balance gate and the render check both catch. |
| T-06-SC | Tampering | npm / pip / cargo installs | low | accept | This plan installs no packages and touches no manifest or lockfile. `06-RESEARCH.md` §"Package Legitimacy Audit" records the gate as not applicable for this phase; there is no supply-chain surface to gate. |
</threat_model>

## Flagged Assumptions

The spec-less probe fallback's six edge rows are owned by plan 06-01, which carries the substantive DOCS-01/02/03 content; all six are dispositioned there (five as `must_haves.truths`, one — the DOCS-03 `unclassified` row — as an explicit flagged assumption). This plan introduces no new probe rows and drops none.

One local assumption specific to this plan:

- **Mermaid cross-subgraph edge support.** Task 1 routes the notify route's query edge into the `N` node declared inside a different subgraph, rather than declaring a second Notion node. `06-PATTERNS.md` and `06-RESEARCH.md` both state this diagram's dialect already supports cross-subgraph references, but no rendered proof exists in this repo — the current diagram has no cross-subgraph edge to demonstrate it. Task 1's `<human-check>` render pass is where this is confirmed; if the edge fails to render, the fallback is to declare the Notion node inside the Notifications subgraph under a distinct identifier, which changes no locked decision.

## Artifacts this phase produces

No new files, exported symbols, routes, env vars, config fields, CLI commands, or database objects are produced by this plan. `README.md` and `README_KR.md` already exist and are modified in place. Nothing downstream can import, call, or depend on anything created here.

| Artifact | Kind | Consumers |
|----------|------|-----------|
| *(none — documentation-only phase; both target files pre-exist and are edited in place)* | — | — |

<verification>
Run from the repository root after both tasks land:

1. All Task 1 and Task 2 automated gates pass.
2. Both mermaid diagrams render in GitHub's Markdown preview — the single check no grep can substitute for, recorded as a per-wave manual gate in `06-VALIDATION.md`.
3. Optional-feature parity read: Cusdis and the email digest are represented in the same three places with the same optional framing, and neither is presented as a deploy prerequisite.
4. `git diff --name-only HEAD` lists exactly `README.md` and `README_KR.md` for the whole of Phase 6. A third file in the diff is a scope violation.
</verification>

<success_criteria>
- D-10 satisfied: the mermaid diagram, Core Services table, and Features list in both files all reflect the email feature.
- D-11 satisfied: the cron path is a separate `Notifications` / `알림` subgraph, not a node added to the Application Layer.
- The optional-feature four-touchpoint pattern established by Cusdis is extended, not replaced — the one deliberate divergence (a self-contained env var block instead of appending to the shared block) already landed in plan 06-01 under D-03.
- Both diagrams render cleanly; both files hold 6 service rows and 9 feature bullets.
- Exactly two files modified across the entire phase; no source, config, or infrastructure file touched.
</success_criteria>

<output>
Create `.planning/phases/06-documentation/06-02-SUMMARY.md` when done.
</output>
